# General
.DS_Store
Thumbs.db
nul

# Editors
.idea/
.vscode/
*.swp
*.swo
.cursorrules
.claude/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node
node_modules/
dist/
dist-ssr/
build/
.env
.env.local
.env.*.local
coverage/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environments
venv/
.venv/
env/
ENV/
env.bak/
venv.bak/

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nohup/

# Docker
.dockerignore

# Artifacts & Documentation
*_artifact.md
design_handoff_lexchat_option_b/
graphify-out/

# Secrets / keys (never commit; rotate if ever tracked)
ollama_auth/

# Playwright MCP browser-session output. Its accessibility snapshots dump the
# DOM value of every field including password inputs, so a snapshot taken on
# Admin Portal -> Developer contains the live provider API key in plain text.
.playwright-mcp/

# Offline Deployment Binaries
binaries/raw/
binaries/offline_dependencies.zip
deployment/certs/Lexchat.pfx

# Local deployment config (machine-specific, never commit)
deployment/local/

# Autostart config is generated by install_autostart.ps1 with machine-specific paths
deployment/autostart_config.ps1

# Loose deployment artifacts
frontend_update.zip

# Pre-pilot replay fixtures — contain lawyers' verbatim research questions.
# Regenerate from the Developer-tab transcript export; see docs/prepilot-fixes/FIX_PLAN.md.
docs/prepilot-fixes/evidence/replay_set.json
docs/prepilot-fixes/evidence/replay/

# Local state written by `python -m tools.replay pin` — records the dev box's
# model and feature flags so `restore` can put them back. Per-machine, and it
# must not be committed or a restore on one box would rewrite another's config.
server_py/tools/.replay_pin_state.json

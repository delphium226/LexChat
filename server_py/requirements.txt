# Pinned to the wheel versions inside binaries/offline_dependencies.zip
# (the air-gapped installer runs pip install --no-index against that bundle,
# so any version change here requires re-running
# deployment/package_offline_native.ps1 and committing new binaries/ parts).
#
# NOTE: pdfplumber and python-docx are NOT in the current offline bundle -
# the bundle predates the document-upload feature. Regenerate the bundle
# before the next fresh air-gapped install.
fastapi==0.135.1
uvicorn[standard]==0.41.0
pydantic-settings==2.13.1
asyncpg==0.31.0
python-jose[cryptography]==3.5.0
passlib==1.7.4
bcrypt==3.2.2
httpx==0.28.1
python-multipart==0.0.22
sqlalchemy[asyncio]==2.0.48
python-dotenv==1.2.2
faker==40.5.1
pdfplumber==0.11.10
python-docx==1.2.0

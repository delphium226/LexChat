# Pre-pilot defect fixes — plan

**Status:** in build. **Branch:** `fix/prepilot-defects` (branched from `main` at `eaaa4ec`).
**Read this file first** at the start of every session, then `SESSION_LOG.md` next to it for
what actually happened, then `evidence/` for the classification this plan is derived from.

Fixing the 14 failure buckets found by re-analysing all 62 pre-pilot sessions (11–21 Aug 2026,
13 lawyers, 377 messages). 25 sessions FAIL, 16 carry a DEFECT, 21 PASS.

Full analysis, with the evidence for each bucket:
<https://claude.ai/code/artifact/43d8e63d-1c9d-4460-907c-2d35c1fa3786>

---

## How to use this file

1. Read **Invariants** — they are the things a fix must not break, and two of them are
   counter-intuitive enough that a cold session will get them wrong without reading.
2. Find the first unticked row in the **Ledger**. Rows are ordered by wave; do not start a
   wave before its predecessor is ticked, and do not start a row whose `Depends on` is unticked.
3. Each row is **self-contained**: files, the change, the acceptance test, and the sessions that
   evidence it. You should not need to re-derive anything from the transcripts to execute a row.
4. Follow the **Verification protocol**. A row is not tickable until its acceptance test passes.
5. Append to `SESSION_LOG.md` before you finish. The *Surprises* line is the one that matters.
6. If reality diverges from the plan, follow the **Re-planning protocol** — amend this file in
   the same commit as the work, do not leave the plan stale.

---

## Invariants

These hold for every row. Breaking one is a worse outcome than leaving a bucket unfixed.

1. **Honest failure is the most-praised behaviour in the corpus and must survive every fix.**
   Three lawyers praised it unprompted, two of them at 5/5 confidence; CambeulW gave 5/5 in
   session 6374 *because* AILA said it could not find something. Every fix to B3 and B5 must
   make the negatives **true** — it must not make the tool more willing to guess. A fix that
   raises the answer rate by lowering the evidence bar is a regression even if the ledger row
   goes green.

2. **Code enforcement beats prompt obedience.** This is already the house style (Phase-2 nudges,
   the parliamentary search budget, the `local_prompt_cache` drafting override). Where a row
   offers a choice between "tell the model to" and "make it so", take the second. B3(b), B7 and
   B12's disclosure footer are all specified this way deliberately.

3. **Do not change retrieval and measure in the same step.** Wave 1 fixes the input to every
   other measurement — while `_matches_jurisdiction` returns `False` for every real extent
   value, no retrieval-quality number means anything. Baseline first (Wave 0), then Wave 1,
   then re-baseline before Wave 3.

4. **A single replay pass is not evidence.** The model is stochastic. Acceptance for any row
   whose test involves an LLM is **n=3, all three clean** for a session classified FAIL, and
   **n=1** for one classified DEFECT (see *Replay configuration* — DEFECT symptoms are mostly
   deterministic, and an ambiguous one gets promoted to n=3). Rows marked *deterministic* have no
   model in the loop at all and need n=1 regardless.

5. **Additive and fail-soft, as everywhere else in this codebase.** No row here justifies a
   breaking change to a stored shape. New columns default; new fields are optional; a failure
   in a diagnostic must never fail the research run.

6. **The lawyers' own words are the ground truth, not the feedback scores.** Two sessions
   (6376, 6361) carry `Q7a = yes` with free text saying the opposite. Where a row cites a
   session, read the transcript, not the score.

---

## Data handling — read before adding evidence to this repo

`evidence/classification.json` holds session IDs, lawyer usernames, thread titles and my
diagnosis of each session. This matches the precedent already set in `docs/TODO.md`, which
names the same users and quotes their feedback.

**The raw transcript export is deliberately NOT committed.** It contains the lawyers' full
research questions — live Scottish Government casework. It lives at:

```
C:/Temp/aila-prepilot/pre-pilot sessions/session-transcripts-all-time-2026-09-14.csv
```

and can be regenerated at any time from **Admin Portal → Developer → Session transcripts
export** (all time). A session that needs the replay inputs reads it from there.

**Open question for the deploying organisation:** whether the replay set (question text, verbatim)
may be committed as a regression fixture. Until answered, keep it out of the repo and
regenerate locally. See `SESSION_LOG.md` Session 1.

---

## Verification protocol

Every row states its own acceptance test. Three kinds, and the distinction matters:

| Kind | What it means | n | Where it lives |
|---|---|---|---|
| **Deterministic** | No LLM. A unit test, or a static check over stored output. | 1 | `server_py/tests/` |
| **Replay** | Re-run the original session's question(s) through `/api/system/chat` and assert on the `audit` trace + answer. | 3 | `evidence/replay/` (gitignored output) |
| **External** | Answer comes from a third party, not from code. | — | `SESSION_LOG.md` |

### Replay configuration — decided 2026-09-14, do not vary without saying so in the log

| | Decision | Why |
|---|---|---|
| **Model** | **Pin to `google/gemini-3.1-pro-preview` via OpenRouter** for every baseline and acceptance replay. | All **176** assistant messages in the pre-pilot ran on this model — uniform, no confound. The dev box is currently set to `moonshotai/kimi-k3` with `google/gemini-3-flash-preview` for summarisation; replaying on that measures a different system. `CLAUDE.md` already records model choice as the dominant variable (~10× swing on the same infrastructure). **First session must confirm the model is still served** — it is a preview model and may have been withdrawn. If it has, stop and re-decide; do not silently substitute. **Confirmed served 2026-09-14** (in catalogue, live tool-calling probe returns its own id; `replay.py check` re-runs this). |
| **Model pinning is a DB write, not a request field** | Set `app_settings → provider.openrouter → model`. `replay.py pin` does it and stashes the prior value for `restore`. | `system.py:129` resolves `provider_config.get("model") or body.model`, so the DB row **always** wins and `body.model` is a fallback that never fires. `audit["model"]` reports what actually executed and every run file asserts it against the pin. Sending the model in the request body and assuming it took effect would have silently measured `moonshotai/kimi-k3`. |
| **Summarisation model** | **Left at the dev box's `google/gemini-3-flash-preview` and recorded, not pinned** (user decision, 2026-09-14). | The pre-pilot's value is **not recoverable** — no per-message column records it and that database is not on this machine. Inventing one would be a silent confound in either direction; recording it keeps the gap visible. It is in `runtime_state` on every run file. |
| **Local prompt cache** | **Forced OFF for every replay sweep** (`replay.py pin`); restored afterwards. | It is cross-user and keyed on the raw tool output + canonicalised query, so run 1 populates it and runs 2..n read it back and skip summarisation. The repetitions would stop being independent draws, which is the entire point of n>1 under Invariant 4. `runtime_state` on each run file records the flag state it actually ran under. |
| **Repetitions** | ~~n=3 for the 25 FAIL sessions, n=1 for the 16 DEFECT sessions.~~ **Revised 2026-09-14 (user decision): n=1 across all 41 first, then reps 2–3 only on FAIL sessions whose first pass was ambiguous or disagreed with the frozen classification.** | The rep policy was costed at ~$50; replay now costs ~2.3× the pre-pilot's recorded spend, making a blanket n=3/n=1 pass ~$140 (see *Cost*). Targeted repetition spends the draws where the uncertainty actually is — a session that reproduces verbatim on the first pass does not need three — and keeps Invariant 4's intent, which is that no **FAIL verdict** rests on a single sample. Sessions promoted to n=3 must be named in `BASELINE.md` with the reason. |
| **Cost** | ~**$27** per single full pass *at pre-pilot rates*; **measured ≈2.3× that today**, so ~**$60** for one pass over all 41 and ~**$70–90** for the targeted-n=3 baseline. A blanket n=3/n=1 pass would be ~**$140**, not the ~$50 originally stated. | ~~$50 for the n=3/n=1 baseline~~ — that figure assumed replay cost equals the pre-pilot's recorded per-message spend. It does not: the recorded cost covers only the saved assistant message, while a replay also pays for the Deep Research **planner** call (44 of them in a blanket pass, saved as no message and therefore absent from the CSV) and for whatever extra tool work the current HEAD does. Measured ratio over the first five sessions: 1.04×–8.90×, aggregate 2.34×. Record actual spend per pass in `SESSION_LOG.md` so the estimate stays honest. |
| **Credentials** | OpenRouter key is in the **local DB**, not `.env` — `app_settings` row `provider.openrouter`. `active_provider` is `openrouter`. | Settings are DB-first by design; `.env` holds only startup fallbacks. Change the model via Admin Portal → Developer, or write the `provider.openrouter` row directly. **Restore the dev box's previous model afterwards** (`replay.py restore`) so unrelated work is not silently running on the pinned model. |
| **Auth** | `/api/system/chat` requires `get_current_user` (`routers/system.py:76`). Log in at `/api/auth/login` and send `Authorization: Bearer <token>`. | The July 2026 auth hardening locked every endpoint and there is no global middleware. ~~carry the cookie~~ — the cookie works, but `POST /api/auth/login` also returns the token in the response body and `get_current_user`'s header branch exists specifically for machine callers (`auth.py:118-122` says so). The header is the documented machine path and avoids the `secure`-flag/scheme coupling entirely. |
| **Deep Research needs two calls** | A DR turn must `POST /api/research/plan` first, then send the returned plan as `deep_research_plan`. | `/api/system/chat` returns **HTTP 400** for `chat_mode="deep_research"` with no plan, by design, so a harness cannot bank a standard research run as a DR result. **The pre-pilot's approved plans are not recoverable** (`messages.research_plan`, on a database not present here), so a replayed DR turn runs a freshly drafted plan — adding planner stochasticity the original did not have. The drafted plan is saved in the run file. |

**The replay harness is `/api/system/chat` with `emit_tool_details=True`**, which emits the
structured `audit` event (`delegations[] → tools[] → api_calls[]`, with `raw_result` alongside
`final_result`). Spec: `docs/api/AUDIT_TRACE.md`. It exists precisely so a harness can see what
was *retrieved*, not just what was answered — which is what most of these rows assert on.
Do **not** build a new harness against `/api/chat`; it does not emit tool events.

`request_timings.source` is already tagged `"eval"` for this endpoint, so replay traffic stays
separable from live traffic in the Efficiency and Cache tabs. Note the dashboards do not yet
filter on it, so a large replay sweep moves the headline numbers — expected, not a bug.

---

## Re-planning protocol

This plan is expected to change. Two rules:

- **Amend the plan in the same commit as the work that invalidated it.** A session that
  discovers a row is wrong edits the row, adds a `~~strikethrough~~` note saying what was
  believed and what is now known, and says so in `SESSION_LOG.md`. A stale plan is the specific
  failure mode this structure exists to prevent.
- **New findings become new rows, not scope creep inside an existing row.** If executing P1.1
  uncovers a second defect, add P1.5 rather than widening P1.1 — otherwise the ledger stops
  reflecting what was actually verified.

Wave 5 rows are **external** and have unknown lead times. Open them early and in parallel;
they are not blockers for anything in Waves 0–4.

---

## Ledger

Status: `[ ]` not started · `[~]` in progress · `[x]` done and accepted · `[-]` dropped (say why)

**Recommended order as at 2026-09-16 (updated during Session 9):** **P2.5 is BUILT and closes B4**, the last partial bucket in Wave 2 alongside B5 — it also closed a trap P3.5 left open (`Commencement Order` is not the subject's commencement; see both rows). **P2.4** remains the cheapest and its evidence has only grown. **P2.9** is still a one-line fix to a measured P2.2 defect and still moves P2.2's published numbers. **P2.7**, **P2.8**, **P3.6** and **P3.7** are open; Wave 4 is independently shippable and blocks nothing. ~~**Recommended order as at 2026-09-16 (updated during Session 8):**~~ **P3.5 is DONE** — the largest bucket is retrieved rather than disclosed, and `/amendment/search` is the fourth LEX endpoint AILA calls. **P2.5 is the row it most changed**: the commencement date is reachable today by a second hop into the commencing instrument's `description`, and one acceptance rep made that hop unprompted — so P2.5 is now about making it reliable, not about finding a route. **P2.4** remains the cheapest, and P3.5's acceptance produced fresh evidence for it (6409 turn 10 stopped questioning a correct citation). **P2.9**, **P2.7**, **P2.8**, **P3.6** and **P3.7** are all available. ~~**Recommended order as at 2026-09-16 (updated during Session 7):** **P2.3 is DONE**, so **P3.5** (`/amendment/search`) — the highest-value row on the page — is **UNBLOCKED and is the one to take next**.~~ ~~Recommended order as at 2026-09-15 (end of Session 6): P2.2 is DONE, so P2.3, P2.4 and P2.5 are all open.~~ **P2.4** remains the cheapest (P5.2's probe already wrote its exact wording, and Session 6 verified the two citations it turns on); **P2.7**, **P2.8** and **P2.9** are available in parallel and are well-evidenced — **P2.9 is a one-line fix to a defect in P2.2 that this session measured but deliberately did not take**, because it moves P2.2's published numbers and needs its own before/after. **P1.6, P2.1, P2.2, P2.3, P2.6, P3.5, P4.4, P5.1 and P5.3 are DONE**; **P0.4's re-export is post-push** and blocks nothing. **Wave 5 is external and has unknown lead time; open those rows now, in parallel, since they block nothing** — P5.2 is the live one.

### Wave 0 — Baseline before any change

| | ID | Row |
|---|---|---|
| `[x]` | **P0.1** | **Build the replay runner.** A script that takes a session's user turns in order and replays them through `/api/system/chat` with the session's recorded filters, capturing the `audit` event and the final answer to a JSON file per run. **Files:** `server_py/tools/replay.py` + `replay_set.py` + `replay_report.py` (+ `evidence/replay/` gitignored). ~~Must log in via `/api/auth/login` and carry the cookie~~ — logs in and sends `Authorization: Bearer`, the documented machine path (see *Replay configuration → Auth*). **Model confirmed served 2026-09-14** — in the catalogue and answering a live tool-calling probe as `google/gemini-3.1-pro-preview`. **Acceptance** *(deterministic)*: **PASSED** — 6404 replayed to a 6,653-char answer (pre-pilot: 6,757) over 4 delegations with 8 `search_legislation` calls, `audit["model"] = google/gemini-3.1-pro-preview`, `model_mismatch` empty, $0.32. **Depends on:** — |
| `[x]` | **P0.2** | **Freeze the replay set.** Extract the 41 defect-carrying sessions from the CSV into a local, gitignored `replay_set.json`: session id, ordered user turns, recorded filter state, the bucket(s) it evidences. **Note:** filters are a per-session snapshot — record them, but treat a mismatch as a finding, not an error. **The mismatch was real and material: chat mode is a property of the TURN, not the session** (see `replay_set.py`, which documents the evidence). Deep Research is one-shot — the frontend reverts to conversational on completion — so a `deep_research` session ran one DR turn among several conversational ones, and **not necessarily the first**: 6409 turn 6 of 11, 6406 turns 2 **and** 3 of 12, 6341 turn 7 of 8. Only **20 of the 155** turns are Deep Research. The per-message signal (`messages.research_plan`) is not in the export, so it is reconstructed from the `**Key findings` block that **only** `DEEP_RESEARCH_SYNTHESIS_PROMPT` requests: present in 23/23 known-DR sessions, 0/24 known-non-DR, 0/15 blank-mode. See P0.4 for the durable fix. **Acceptance** *(deterministic)*: **PASSED** — 41 entries, every FAIL/DEFECT session present, no PASS leakage. Three independent cross-checks against the frozen analysis all matched exactly: 10-of-23 DR sessions mentioning the step cap, 15 unanswered turns across 11 sessions, and zero session-mode reconciliation conflicts. **Depends on:** — |
| `[x]` | **P0.3** | **Run the baseline.** ~~n=3 for the 25 FAIL sessions, n=1 for the 16 DEFECT sessions (~$50)~~ → **n=1 across all 41, then reps 2–3 on the 12 sessions the first pass did not settle** (user decision 2026-09-14). **Done: `BASELINE.md`.** **34 of 41 reproduce, 7 do not, 0 inconclusive**; $61.28 total ($37.62 for the n=1 pass over all 41, $23.66 for 24 targeted reps). **The repetitions overturned three `does not reproduce` verdicts** (6357, 6389, 6396 — 6396's rep1 halted four workers where the single draw had answered cleanly), which is the result that justifies Invariant 4 on this corpus. Every `does-not-reproduce` is in the n=3 set, because a single draw cannot establish a negative (Invariant 4). **Three findings that change later rows:** (a) B2 fails *selectively*, not closed — see the amended P1.1; (b) the halt reaches users through the **Manager** path too, and 6383 turn 1's entire answer was the raw `[Research halted: exceeded 20 tool-call steps]` string — see the amended P2.1; (c) three of the nine non-reproductions are **not** improvements (6357 swapped a halt for a filter failure, 6381 discloses honestly but retrieves worse, 6340 swapped fabrication for a truncated non-answer). **Depends on:** P0.1, P0.2 |
| `[~]` | **P0.4** | **Export the Deep Research flag instead of inferring it.** *(New row, from P0.2 — non-blocking.)* **DELIBERATELY SKIPPED so far, and it is NOT the next row despite being the first unticked one** — Waves 1 and P1.5 both ran without it and the *Read this file* rule about starting at the first unticked row does not override an explicit deferral. **Do it before the next replay sweep, not before the next code row:** it removes confound 5 ("which turn ran Deep Research is inferred") from every future measurement, and Wave 2's acceptance is replay-based at n=3. Its code half can be done any time; the re-export half is blocked on target access. The transcript export has no per-message Deep Research column, so which turn ran Deep Research has to be reconstructed from the answer text. The inference measured perfectly on this corpus, but it is read off the **answer**, so it is structurally blind to a DR turn that produced no answer — and 15 turns across the corpus got no reply, which is bucket B13 itself. Add a per-message "Deep research" column to **Admin Portal → Developer → Session transcripts export**, sourced from `messages.research_plan` (the same signal `feedback.py::_session_mode` already uses at thread level — note its `IS NOT NULL` caveat about SQLAlchemy JSON nulls), then re-export. ~~**Blocked on target access for the re-export**, not on the code change.~~ **Worse than that, established 2026-09-15: the re-export is blocked until the FINAL PUSH.** The pre-pilot data exists only on the target (the dev database holds 42 dev messages, none of them pre-pilot), and the new column only exists in this branch's code — which the target cannot have until the whole-plan-then-one-push policy lets it. There is no local route either: `install_backup_task.ps1` has never been run, so **no backup exists to restore** (see `CLAUDE.md` → Backup & Scoped Restore). **So P0.4's re-export is a POST-PUSH activity and cannot inform any sweep taken before then.** The ledger's "do it before the next sweep" note is therefore unachievable as written and should not hold up P2.2 or any Wave 2 acceptance — confound 5 (which turn ran Deep Research is inferred) simply persists for the remaining pre-merge sweeps, as it did for `wave1/` and `wave2_p21/`. It measured perfectly on this corpus, so the cost is bounded and known. **Acceptance** *(deterministic)*: a re-exported CSV whose per-turn flag agrees with `replay_set.py`'s marker on all 62 sessions, or a recorded list of where they differ. **Depends on:** — | **CODE HALF DONE (Session 5); the re-export is still blocked, so the row stays open.** `TranscriptMessageOut.deep_research` + `_ran_deep_research` (`routers/feedback.py`) and a **Message deep research** column on the CSV (`sessionFeedbackExport.js`), `client/dist/` rebuilt. Sourced from `messages.research_plan` — the same observed signal `_session_mode` already prefers over the filter snapshot, with the same `IS NOT NULL` trap, tested in Python here because the rows are already loaded. **Per TURN, not per thread**, which thread-level `session_mode` cannot give: a thread that mixed the modes reports `deep_research` for all of its turns. Pinned by `test_transcripts_flag_deep_research_per_turn_not_per_thread`. **Deviation to record: this session ran a replay sweep (P2.1's acceptance, `wave2_p21/`) WITHOUT the re-export**, against the ledger's "do it before the next sweep" — the re-export needs target access, the replay set was frozen at P0.2, and re-freezing it was not available. Confound 5 therefore still applies to `wave2_p21/`; it does not touch P2.1's acceptance, which is graded per halted turn on signals that do not depend on knowing the mode.

### Wave 1 — The proven code defects (deterministic, no model behaviour change)

| | ID | Row |
|---|---|---|
| `[x]` | **P1.1** | **B2 — Fix the jurisdiction filter.** `_matches_jurisdiction` (`server_py/src/agent/tools/lex.py:44`) splits `extent` on `+` and tests single-letter tokens `E`/`W`/`S`/`NI`. The LEX API returns full territory names. Verified live: the six values the API emits are `['']` (68), `['Scotland']` (42), `['England','Wales']` (4), `['England','Wales','Scotland']` (3), `['United Kingdom']` (2), `['Northern Ireland']` (1) — all six return False for every jurisdiction value. ~~so the filter discards 100% of results~~ **Corrected by the P0.3 baseline, and the correction matters: the filter does not fail closed, it fails *selectively*.** `_matches_jurisdiction` opens `if not extent: return True`, so rows with a **missing** extent pass. Of the **1,009 rows that survived** a jurisdiction filter across the whole baseline, **every one had `extent: []`** and **not one carrying a real extent value survived** — a Scotland filter drops `['Scotland']` and keeps the unknowns (6354 returned the *Building Materials and Housing Act 1945* under `jurisdiction=scotland`). That is why 13 sessions ran with it and nobody reported a broken filter: it returns a plausible non-empty result set that is wrong rather than an obviously empty one. **Measured baseline: 351 of 974 searches (36%) returned nothing to the model, 478 (49%) demonstrably lost rows, ≥2,147 rows lost; 0 of 557 searches lost anything with the filter off.** So the row has **two** jobs, not one: map the real vocabulary, **and decide what `extent: []` means** — leaving unknown-and-include is what makes the failure invisible. `['']` should join it (most SSIs carry it). `uk_wide` currently requires `tokens ⊇ {E,W,S,NI}` and is equally unreachable; decide what it means against `['United Kingdom']`. **Decide explicitly** that UK-wide and assimilated (`eur`) instruments are in scope for a Scotland filter — that is what CambeulW asked for in 6406 and it is the difference between a usable filter and a trap. **Acceptance** *(deterministic)*: unit test asserting True for each live extent value against its own territory, pinned to a fixture of the live vocabulary so an API change breaks the test not the product; plus a `jurisdiction=scotland` search for "Courts Reform (Scotland) Act 2014" returning `asp/2014/18`; plus `replay_report` showing 0 searches emptied by the filter on a re-run of 6354/6381/6408. **Evidence:** 6408 6384 6383 6382 6381 6396 6359 6407 6354 6357. **Depends on:** P0.3 | **DONE.** Policy decided with the user 2026-09-14: the control means **"applies in"**, not "made for" — Scottish-extent + UK-wide + unknown-extent, minus other-jurisdiction id prefixes. Scored over the baseline's 17,560 real rows it drops **0%** of unambiguously Scottish rows and admits **0** clearly non-Scottish ones, against the old code's **97.5% dropped**. Note `['']` had to be treated as unknown-and-include: matching territory names alone would have dropped 40% of Scottish material (2,009 Scottish SIs carry `['']`) — a new trap. The id-prefix tie-break (`nisr/`, `wsi/`) is what makes admitting unknowns safe. **Acceptance passed:** `tests/test_jurisdiction_filter.py` (42 tests, live vocabulary pinned as a fixture); live `jurisdiction=scotland` search for "Courts Reform (Scotland) Act 2014" keeps 20/20 incl. `asp/2014/18`, where the old code kept **0/20**.
| `[x]` | **P1.2** | **B4 (filter half) — Stop the `current_only` filter pretending.** `_INACTIVE` (`executor.py:211–216`) tests for `{repealed, revoked, spent, expired, not in force}`; ~~the API's `status` vocabulary is `final` and `revised` only~~ — **corrected at P2.5: it is `final` 60.3%, `revised` 38.8% and `stub` 0.9%** over 15,160 model-visible rows (`lex_probe --inforce`), three values rather than two, and none of them an in-force signal — so nothing is ever excluded. The field means *which text version is held*, not in-force status, so this **cannot be fixed by extending the word list**. Two parts: (a) remove or relabel the filter so it does not promise currency it cannot deliver; (b) strip in-force assertions from answers — see P2.5 for the model-side half. **Do not** silently leave the toggle in the UI doing nothing. *Open product call when this row is reached:* remove the toggle outright or relabel it to what it actually does. Recommendation is **remove** — a relabelled filter that still excludes nothing is a second trap, and 42 of 62 sessions ran with this on believing it worked. **Acceptance** *(deterministic)*: unit test pinning the live status vocabulary; a search with the filter on returns the same set as with it off, *or* the filter is gone. **Evidence:** 6378 6341 6411. **Depends on:** P0.3 | **DONE — removed, per the user's decision 2026-09-14.** **The row understated this: the filter was not merely a no-op, it was actively asserting currency in two places.** The UI pill read *"In force as at <today>"*, and `build_filter_constraint_block` injected *"Status: In-force legislation only. Do not cite or rely on repealed or not-yet-in-force legislation."* into the system prompt. 42 of 62 sessions ran with that on, so **this is the proximate cause of bucket B4** and it partly pre-empts P2.5 — the model was reporting currency because the system told it the results were current. Removed: the toggle, the pill, the prompt claim, the no-op post-filter **and the request field** (option C, user 2026-09-15). Safe for clients because `AgentRequestBase` does not set `extra="forbid"` — pydantic ignores an unknown key rather than rejecting it, so a client still sending `current_only` gets the same 200 and the value goes nowhere. `audit["filters"]["current_only"]` is deliberately **kept, always null**, so the trace shape is unchanged for the external eval harness and `AUDIT_SCHEMA_VERSION` stays at 1; the natural moment to go to v2 is P2.1, which changes the trace shape anyway. `docs/api/AUDIT_TRACE.md` updated. **Acceptance passed:** `tests/test_current_only_removed.py`; frontend rebuilt.
| `[x]` | **P1.3** | **B5 (data half) — Stop discarding what the search actually found.** Two changes in `executor.py`: (a) `_TYPE_CODES` (`lex.py:80`) has no `eur` entry, so `legislation_type` drops every assimilated EU regulation — confirmed present and retrievable (`eur/2009/1069` returns 10 sections); (b) `executor.py:227` sets `slimmed["total"] = len(results)` **after** post-filtering, destroying the API's real match count. **Baseline: 1,010 of 1,531 searches misreport `total`, including 438 of 557 with *no* jurisdiction or type filter — so this is not a side-effect of B2 and does not go away if P1.1 lands first.** **Measurement warning:** `executor.py` also caps every search to `results[:5]` *after* filtering. That cap is deliberate slimming, not a defect; any "rows lost" figure that ignores it attributes ordinary truncation to the filters and overstates B2 several-fold. `replay_report.RESULT_CAP` encodes it — a search returning exactly 5 is cap-bound and is evidence of nothing. Return `returned` / `total_matched` / `removed_by_filters` as separate fields so the model can tell "5 matched" from "172 matched and your filters removed all but 5". This is the D16 defect-2 fix, now with field evidence. **Acceptance** *(deterministic)*: unit test asserting the three counts are distinct and correct for a filtered search; `legislation_type=secondary` no longer drops an `eur/` result, or `eur` is deliberately excluded with the reason in a comment. **Evidence:** 6406 6408 + the whole of B5. **Depends on:** P0.3 | **DONE.** `_TYPE_CODES` gains `eur`/`eudn`/`eudr` (assimilated EU law sits with secondary — directly-applicable law made outside Parliament), plus `nisro`/`nisi`/`sdsi` which were missing for the same reason. Counts now reported as `returned` / `total_matched` / `removed_by_filters`, with `filters_applied` naming the filters when any removed rows, and `total` restored to the API's real match count. **Acceptance passed:** `tests/test_search_result_shape.py`.
| `[x]` | **P1.4** | **B14 — Provision links must point at the provision.** Across all 62 transcripts, 9 of 293 section-labelled legislation.gov.uk links resolve to the Act's contents page. ~~The baseline puts it far higher: 88 of 376 (23.4%)… worst offenders 6406 (43/61), 6389 (13/18), 6375 (11/15), 6341 (10/53)~~ — **that was my detector, not the product.** `PROVISION_LABEL` read the word "regulations" plus digits, so every SI cited by title ("The X Regulations 2013") was scored as a reference to "regulation 2013"; **73 of the 88 were this false positive**, several of them on links that were correct. Corrected 2026-09-15 during P1.5: **20 of 376 (5.3%)**, worst offenders 6406 (16/61), 6375 (3/15), 6389 (0/18), 6341 (0/53). B14 is real but roughly a quarter of the size first published, and 16 of the 20 sit in one session. **P1.4's acceptance is unaffected** — it passed on `tests/test_search_result_shape.py`, which tests the product. Note the warning: `test_bad_link_*` in `tests/test_replay_tooling.py` was green the whole time, because it used synthetic labels that never contained a real SI title. The acceptance check already exists and already runs: `replay_report`'s `bad_links`, pinned by `tests/test_replay_tooling.py`. Build the URL from the provision identifier rather than letting the model compose it. HeatherE asked for exactly this in 6334. Lowest-effort row on this page and it directly serves the click-through verification the lawyers actually use. **Acceptance** *(deterministic)*: static check over any replay output — every link whose label names a section/regulation/article/schedule contains the matching path segment. **Evidence:** 6334 6409 6375 6341 6348. **Depends on:** P0.3 | **DONE, and the cause was our own prompt.** The API already returns a provision-level URL per result (`uri`), but `search_legislation_sections` passed its response through raw and `WORKER_SYSTEM_PROMPT` told the model *"you MUST manually append `/section/{number}` to the Base URI"* — it was obeying an instruction to guess. New `_slim_section_results` hands over the API's own URL under a `url` key (also normalising `legislation_id`, which the two LEX endpoints spell differently), and both citation protocols now say to use it verbatim. Covers schedules, regulations and articles, not just sections. **Acceptance passed:** `tests/test_search_result_shape.py`. **Re-measured at P1.5, and the headline metric misreports it.** `bad_links` went **20 to 32**, which reads as a regression and is not: that metric asks whether a link matches the *granularity* its label names, and cannot ask whether the URL was ever **returned by a tool**. On the question that matters, ~~provision URLs that no tool ever returned went **327 of 327 (100%) to 26 of 136 (19%)**.~~ **Corrected at P1.6 — that reading was the instrument again.** It asked whether the URL appeared in `final_result`, which is the **summarised** text whenever summarisation fired, and 70% of section searches are summarised — the summary keeps the section numbers and drops every URL. Split three ways (shown to the model / reconstructed after summarisation ate it / never returned at all), rep-1 like-for-like: shown **5 → 110**, reconstructed **293 → 26**, and provision URLs no tool returned **anywhere** **29 → 0**. P1.4 did more than was published, not less — it slimmed the payload enough that 81% of cited provision URLs are now copied verbatim from a result the model was actually shown, against 1.5% before. The old prompt *told* the model to append `/section/{number}`, so nearly every Wave 0 provision URL was composed rather than copied, and most carried the right number so the checker scored them **good**. A manufactured URL resolves to a real page and reads to a lawyer as a verified citation, which is worse than a link that misses its provision. The 32 flagged links are the residue of honesty: with no retrieved provision URL and forbidden from inventing one, the model links the Act's contents page while naming a section — in 6348, **zero** provision URLs were returned by any tool yet it cites FOISA ss.36 and 55, so the bad link is now a *symptom of citing an unretrieved provision* rather than a formatting fault. New signal `provision_links_manufactured`. The residual 19% is a live defect and becomes **P1.6**.
| `[x]` | **P1.5** | **Re-baseline after Wave 1.** Re-run P0.3 on the Wave 1 HEAD. Wave 1 changes what every search returns, so the Wave 0 numbers no longer describe the system (Invariant 3). **Runbook — four steps, and three of them have a trap:** **(1)** `cd server_py && python -m tools.replay pin` — re-pins the model and turns the local prompt cache off. The dev box was deliberately **restored** after the Wave 0 sweep, so it is currently on `moonshotai/kimi-k3`; running without pinning measures a different system and the runner will refuse unless you pass `--allow-unpinned`. **(2)** Start uvicorn **fresh** — `python -m uvicorn src.main:app --host 127.0.0.1 --port 8000 --no-access-log`. *Python loads modules at import*, so a server left running from a previous session serves the OLD code and would record pre-Wave-1 behaviour as the Wave 1 result. Confirm with `python -m tools.replay check`. **(3)** `python -m tools.replay run --all --reps 1 --out-dir ../docs/prepilot-fixes/evidence/replay/wave1` — **a NEW directory**. `run` skips any run file that already exists, so pointing it at `…/replay/baseline` silently skips all 65 and does nothing. ~$38, ~6 h serial. **(4)** `python -m tools.replay_report --dir ../docs/prepilot-fixes/evidence/replay/wave1 summary`, then `python -m tools.replay restore`. **Read it session-by-session, not as two headline numbers:** a verdict can flip for the wrong reason. 6381 currently reads *does not reproduce* because it honestly says it cannot find the Victims and Witnesses (Scotland) Act 2014 — and the reason it cannot is B2 emptying its searches. With P1.1 in it may now answer, which looks like a Wave 1 win but is the removal of a cause already recorded. **Numbers to watch:** B2's 351 emptied searches → expect 0; B14's 88/376 bad provision links → expect a collapse; **B4's 27 in-force claims is the interesting one** — P1.2 removed the prompt line that caused them, so a sharp fall means P2.5's scope shrinks before it is written. **Acceptance:** `BASELINE.md` gains a second column. **Depends on:** P1.1–P1.4 | **DONE 2026-09-15.** 41 sessions / 155 turns on the Wave 1 HEAD, **$27.22 / 3.7 h** against $37.62 / 6.0 h, zero model mismatches, zero errored turns, no product code changed during the sweep. **B2 and B5 closed**: searches emptied by filters **351/1,531 to 3/790** (the 3 are `legislation_type=primary` correctly excluding SSIs, checked row by row); `total` misreported **1,010 to 0**. **B2 was also a cost and latency defect** — searches **1,531 to 790**, delegations 278 to 197, and the sweep ran 2.3 h faster for the same turns, because an emptied search made the model reformulate and retry (6396 172 searches to 1, 6381 80 to 6). **B4 rose 27 to 30, so P2.5's scope does NOT shrink** — the prompt instructs the claim at three sites P1.2 never touched. **Silent halts rose 2 to 5** while halt text fell 9 to 6, with no code change: stochastic, and the exact trade P2.1's rewritten acceptance guards against. **Cap pressure rose** (p90 15 to 20; at-or-over-cap 6.3% to 11.2%) — see P2.1. Three detectors were corrected mid-sweep (B14 six-fold over-count, B1 blind to a third of halt disclosures, B8 conflating two conditions); both columns use the corrected instrument. **n=1 throughout, so no session is recorded as fixed** — Invariant 4. |

| `[x]` | **P1.6** | **B14 residual — enforce the provision URL in code, not in the prompt.** *(New row from P1.5's re-baseline, per the re-planning protocol: P1.4 is done and accepted, this is a separate, newly-measurable defect.)* ~~P1.4 cut manufactured provision URLs from **100% to 19%**… The residual 19% is the model disobeying that instruction where it holds only an Act-level URL — 6365 appends `/section/21` and `/section/22` to `asp/2000/1`, for provisions no tool retrieved.~~ **That premise was the instrument, not the product — the ninth trap, and it was reading a third of the answer.** `_urls_returned_by_tools` asked only whether the URL appeared in `final_result`, which is the **summarised** text whenever summarisation fired — and **70% of section searches are summarised**, the summary keeping the section numbers and dropping every URL. 6365's `/section/21` and `/section/22` **were** returned by `search_legislation_sections`; they survive in `raw_result` and were eaten before the model saw them. Corrected, there are **three** outcomes, not two: *shown* (copied verbatim), *reconstructed* (the tool returned it, the summariser dropped it, the model rebuilt it — substantiated but built by guessing), *manufactured* (no tool returned it anywhere — the provision was never retrieved). Rep-1 like-for-like, Wave 0 → Wave 1: shown 5 → 110, reconstructed 293 → 26, **manufactured 29 → 0**. So the published "327/327 (100%) → 26/136 (19%)" conflated the last two and **understated** what P1.4 achieved. **What P1.6 therefore fixes is the mechanism, not a live wrong answer**: 19% of cited provision links are still *rebuilt* rather than copied, they happen to be right today, and nothing made them so. **Invariant 2 applies exactly as written: stop instructing, start enforcing.** **Built, both halves** (`src/utils/citation_links.py`): **(a) the cause** — `provision_url_block` appends the retrieval's own provision URLs *after* summarisation, for the same reason the Phase-2 nudge is appended there (the summariser cannot eat what it never saw), so the model copies instead of rebuilding; **(b) the guarantee** — `enforce_provision_links` runs at the worker-report seam, the Manager's final-answer seam and the Deep Research synthesis seam, against a **request-scoped** set of every legislation.gov.uk URL any tool returned (harvested from the RAW result, and shared across delegations and DR steps because a provision retrieved in step 2 is legitimately cited in step 5). A provision URL not in that set is **demoted to the Act when the Act was retrieved** and unlinked otherwise, with one footnote. ~~Prefer (b)~~ — both, and (b) is not "a filter over model output" in the drift sense: it is keyed on retrieval provenance, not on wording. **The row's warning held and shaped the design:** the fix must not simply strip links (when only the Act is held, linking the Act is correct — the error is the label naming a provision), and the marker must be **true**: we know no tool returned a URL for the citation; we do **not** know the provision went unread, because a whole-Act `get_legislation_text` carries one URL and no per-section ones. So the footnote asserts only what is known about the link (Invariant 1). **Acceptance** *(deterministic)*: ~~`provision_links_manufactured == 0` over a replay directory, plus a unit test~~ — the first half was **already true at Wave 1** once the detector was corrected, so on its own it would have passed without any code being written. **PASSED on the restated test:** `tests/test_citation_links.py` (28 tests, incl. end-to-end through `run_worker_agent`: identical model output, identical report, retrieved → untouched vs not-retrieved → demoted) plus `tests/test_replay_tooling.py`'s seven new provenance tests, which pin the corrected detector *and* pin that P1.6's own block counts as **shown** — without that last one the fix would have been invisible to its own metric. `provision_links_manufactured == 0` holds over `evidence/replay/wave1` and is now enforced by code rather than observed. **Watch `provision_links_reconstructed` (26 at Wave 1) on the next sweep — that is the number this row moves**, and it needs a Wave 2 sweep, not this row. **Evidence:** 6365 6348 6407. **Depends on:** P1.5 |

### Wave 2 — Structural honesty (changes what the model may say, not what it can find)

| | ID | Row |
|---|---|---|
| `[x]` | **P2.1** | **B1 — The research halt must stop being a legal finding.** ~~"The **Deep Research** halt"~~ — **the halt is not confined to Deep Research.** `chat_loop` returns `"[Research halted: exceeded {max_turns} tool-call steps]"` as the worker's **assistant content** (`agent/openrouter_client.py:158`, `agent/ollama_client.py:85`, `max_turns=20`), so it escapes by **both** routes: `run_deep_research` (`agent_core.py:698`) treats it as a step's findings and hands it to synthesis, **and** a plain Manager `delegate_research` gets it as the worker's report. Confirmed in baseline replay of **6340**, a standard `research`-mode session (`kind: "delegation"`, no step), where a 30-tool-call worker halted and the lawyer was told the agent *"exceeded its operational limits (timed out)"*. **A fix at the `run_deep_research` site alone leaves the Manager path open.** **10 of 23 DR sessions (43%)**, plus an unmeasured share of standard sessions — count them from the baseline. Return the halt as structured metadata on the step/worker result (`{complete: false, reason: "step_budget"}`); synthesis and the Manager must not render it as a legal gap; the UI marks the step incomplete. **Invariant 1 applies with a sharp edge here:** 6340's halt answer *reads* as honest failure but says "timed out" (it hit a step cap, not a timeout) and then invents a cause — *"a broad enabling power has generated a very large volume of statutory instruments over several decades"* — for an Act that is a **404 in LEX**. The disclosure must be true, not merely non-legal. **Read `request_timings.max_turns_halted` (already collected, `utils/stopwatch.py:177`, already aggregated at `routers/stats.py:673`) before changing the cap** — the cap question is separate from the presentation question and only one of them is urgent. **Cap pressure measured at P1.5, and Wave 1 moved it — do not decide this on the Wave 0 numbers.** Over the 30 sessions of the Wave 1 sweep, like-for-like against the same sessions in the rep-1 baseline: delegations **190 to 134** (-30%, the model stopped flailing against empty searches), but **p90 tool calls per delegation 15 to 20 — the 90th-percentile delegation now sits AT the cap** — and delegations at or over 20 went **6.3% to 11.2%** (12 to 15 in absolute terms, while the denominator fell by a third). Median 4 to 5, max 41 to 45. **Fixing retrieval nearly doubled the share of delegations hitting the ceiling**, because a search that returns results generates Phase-2 retrieval work where an emptied search just got retried. This is Invariant 3 one level up: the cap was parked behind a metric that was itself measured against a broken filter. **Still do not raise it reflexively.** Hitting the cap is not self-evidently wrong — a delegation needing 25 steps may be badly scoped rather than starved, and 20 to 30 would add cost and latency to the ~11% without necessarily improving an answer, while *masking* the very failure this row exists to make honest. What has changed is that the cap is now **binding** rather than a rarely-touched safety net, so it is a live design decision. Decide it here, with the presentation fix, on the completed sweep — not before. (Numbers: n=1, 30 of 41 sessions, taken mid-sweep with the halt-heavy sessions 6382, 6384, 6396, 6407, 6408 still outstanding; refresh before deciding.) ~~**Acceptance** *(replay, n=3)*: 6406, 6382, 6408 and 6340 produce no halt text and no invented cause in the report body.~~ **REWRITTEN 2026-09-15 during P1.5 — the old test was broken three ways and one of them made it actively harmful.** **(a) "No halt text" is the wrong assertion.** It is satisfied by saying nothing, and **2 turns in the rep-1 baseline (3 across all runs) already halt silently** — a worker halts, a normal-looking report comes back, the lawyer gets no signal it is incomplete (6396 rep1 halted **four** workers and never mentions it). Under Invariant 1 that is worse than the text leaking, so the trivially-passing implementation of the old test is the worse product. **(b) The detector was blind to a third of the wordings** — `HALT_PARAPHRASE` caught 9 of 14 real disclosures, missing "exceeded its *processing* limits", "timed out while searching", "exceeded the maximum *permitted steps*" — so the assertion could pass on a paraphrase it did not know. Widened and re-validated at P1.5. **(c) 6406 no longer halts at all** (0 of 4 steps in all three reps), so it cannot evidence the fix; **6382 and 6384 still halt and still leak**, and **6383 turn 1 is the strongest case** — the raw string as its entire answer, and note it **never appears in any delegation report**, so a check reading only `delegations[].report` is blind to it. **New acceptance** *(replay, n=3, on 6382, 6384, 6383 and 6340 — not 6406)*: for every turn whose worker halted, (1) `halts_undisclosed == 0` — the answer *does* tell the lawyer the research was incomplete; (2) no raw `[Research halted` string anywhere in the answer; (3) the stated reason is **true** — a step cap, not "timed out"; (4) no invented cause (6340's *"a broad enabling power has generated a very large volume of statutory instruments"* for an Act that 404s in LEX); (5) the halt is present as structured metadata on the step/worker result. Assert on the **answer**, not only the trace. 6406 is the acceptance case — all four steps halted and the report cost $2.36 to say nothing. **Evidence:** 6406 6408 6382 6384 6409 6374 6407 6389 6357 6341 6340 — and note **6409 turn 7 is the starkest case in the corpus, stronger than 6340**: 25 Phase-1 searches, 0 retrievals, the worker hit the cap, and the entire answer was *"The research agent could not locate [the SSI] in the legislation database"*. A step cap rendered as a **negative retrieval finding**, with no hint the research was cut off — an *untrue* honest failure, which is the one thing Invariant 1 cannot survive. **Depends on:** P1.5 | **DONE (Session 5) — code enforcement at three seams, and the cap decision went the other way.** **Built:** `chat_loop` (both providers) now returns `{"halted": {reason, limit, steps}}` alongside the marker, so the halt travels as structure; `run_worker_agent` replaces a halted report with a statement addressed to the agent that will read it (modelled on the proven `[Research Agent Error]` containment string) which forbids the two behaviours 6340 showed — calling it a timeout, and inventing a cause; `process_user_request` **and** `run_deep_research` strip every raw marker and **prepend** a code-emitted notice naming the plan steps where known. All three escape routes covered, including the Manager's own loop. **The disclosure is unconditional** — no detector decides whether the model already disclosed it well enough, because a false negative there is a lawyer relying on a partial answer, and on this work the detectors have been wrong more often than the product. Rides out as `research_incomplete` on the result (additive, carried by the existing `result` SSE event, no router change) and as `delegations[].halted` in the audit trace — **`AUDIT_SCHEMA_VERSION` bumped to 2**, spec updated, and the test now pins the constant rather than a literal. `src/utils/research_halt.py`. **The UI half is satisfied by the text, deliberately, and a badge would be worse:** `research_incomplete` has no DB column, so a badge would vanish on reload while the prepended notice — which lives in `messages.content` — persists. A marker that disappears on reload teaches the lawyer that its absence means complete. **THE CAP: NOT RAISED, and the row's own figures were the wrong counter.** `max_turns` caps **ReAct rounds**; a round issues several tool calls in parallel (`asyncio.gather`) and the Worker prompt instructs batching, so counting tool calls against 20 measures a different quantity — 6341 turn 6 made **45 tool calls in 13 rounds**. On the **completed** 41-session sweep, using `react_turns_max` (the counter the cap acts on): p90 **17 → 13**, at-cap **7.8% → 7.2%**. Pressure **fell**; it did not nearly double. And the turns that do hit it are **looping, not starved** — 26% redundant tool calls against a 15% base rate, with 6409 spending 22 and 25 Phase-1 searches for 1 and 0 retrievals. More rounds buys more of the same and would mask the failure this row exists to make honest. Full working: `BASELINE.md` → *The step cap*. That evidence opens **P2.7**. **Acceptance:** four of the five conditions are mechanical and are checked by `replay_report … halts` (new subcommand, per **turn** — a session's failure lives in one turn of four); the fifth (no invented cause) is a reading of the prose, printed by `--answers`. Wave 1 grades **11 of 11 halted turns FAILING** as the before-state: **6 of 11 say nothing at all**, and of the 5 that do mention it, 2 call it a timeout and 1 discloses it by printing the raw marker as the answer — so **2 of 11 were disclosed acceptably**, and none carried the metadata. **ACCEPTANCE PASSED** — replay n=3 on 6382, 6384, 6383, 6340 → `evidence/replay/wave2_p21/` (12 runs, **$8.63**, 0 errored turns, 0 model mismatches). **7 halted turns, 0 failing** on all four mechanical conditions, and reading the prose for the fifth, **none invented a cause** — 6340's *"a broad enabling power has generated a very large volume of statutory instruments"* is gone from all three of its replays. **State the coverage honestly: only 6340 and 6382 give full n=3 evidence** (both halted in 3 of 3 reps); 6383 halted in 1 of 3 and **6384 in 0 of 3**, so their condition is vacuous rather than passed, and "four sessions × three reps" would read as four times the evidence it is. Note also that **6383 turn 1 — the row's named strongest case — did not halt in any rep**; the halt moved to turn 4's DR plan. That is why the grading is per halted **turn** over the directory rather than per named session. The model's own prose came along too (not assumed, since the disclosure is code-emitted): all 7 turns state the true reason in the model's words, and 6383 rep 1 adds its own *"this is not a negative result or evidence that the material does not exist"*. **One residual, booked to P2.2 rather than widened into this row:** 6382 rep 2 still opens with a negative (*"no SSIs … were found that contain the '£' symbol"*) drawn from two steps that halted — now contradicted in plain sight by the notice, but not yet prevented.
| `[x]` | **P2.2** | **B5 — No bare negatives.** A zero-result answer must state what was searched, under which filters, and how many matches the filters removed. Also: `search_legislation` is the only search tool with **no zero-result nudge** — case law has one, Hansard has one, SP has one, and the legislation branch fires only `if id_pairs:`. Add the else branch. ~~`agent_shared.py:565` / `:720` / `:737`~~ **line numbers drifted when P1.6 and P2.1 landed; as at Session 5 the three existing nudges are at `agent_shared.py:587` (case law), `:644` (Scottish Parliament) and `:740` (Hansard), and the legislation branch is the `if id_pairs:` at `:757`. Grep `"This search returned 0 results"` rather than trusting any of these.** **Acceptance** *(replay, n=3)*: 6409 and 6367 name their search terms and active filters in the negative answer. **Invariant 1 applies** — the test is that the negative is *explained*, not that it becomes a positive. **Evidence:** 6367 6381 6409 6410 6384 6373 6365. **Amended at P2.1 (Session 5): this row also owns the negative drawn from an INCOMPLETE search, which P2.1 narrows but cannot close.** In 6382 rep 2 of the P2.1 acceptance sweep, steps 2 and 3 halted at the cap and the synthesis nonetheless opened *"no Scottish Statutory Instruments made under section 95 … were found that contain the '£' symbol"* — the two steps that would have established that are the two that halted. The lawyer is now told (P2.1's notice says in terms that this is **not** a finding that the material does not exist, and the BLUF is hedged with "based on the completed portions"), so a contradiction is visible rather than hidden — but the answer still leads with a negative it did not earn. **A negative must state the limits it was reached under, and a halted step is one of those limits**, alongside the filters and search terms this row already covers. Wave 1's 6409 turn 7 is the same failure without the disclosure. **Depends on:** P1.3, P2.1 | | **DONE (Session 6) — and the row's own premise was 0.5% of the bucket.** **What the measurement said before any code was written:** the missing `else:` on `if id_pairs:` fires on **4 of 790** searches post-Wave-1, while **783 of 783** measurable searches are *windowed* (5 rows shown of a median **141** ranked candidates, p90 185, max 220) and **not one of the 44 measured bare negatives followed an empty search**. Every one came from a result set that had results, just not the wanted one — *"no commencement regulations have been made"* is seven of them verbatim, drawn from the top 5 of 141. A fix confined to the empty branch would have moved none of the forty-four and could still have gone green on a loose detector. **Built, at three seams, and the second was found by the acceptance failing:** (a) a scope block on **every** `search_legislation` result — query, filters, the window, and the coverage limb — plus the first nudge `search_legislation_sections` has ever had (6335 turn 7 reported a targeted search "returned no results" when it had returned results, then invented *"the database is having difficulty parsing the Schedule B1 structure"*); (b) `worker_scope_block`, appended in code to the **worker's report**, because the Worker sees tool results while the **Manager sees only the report and the DR synthesis only the step findings** — whoever writes the BLUF had never seen a scope block (6367 rep 1 carried the block in context **27 times** and still said a search *"confirms"* that no SSIs prescribe the detail); (c) `answer_scope_footer`, a one-line lawyer-facing statement emitted by code on every researched answer, plus an unconditional strip so the agent-facing blocks never render. `src/utils/search_scope.py`. **Why (c) exists: (b) was not enough, measured.** Instruction-only got explained negatives from **20% to 56%**; the other 44% still told a lawyer something was not found without saying what had been looked for. Invariant 2 exactly. **ACCEPTANCE PASSED with one stated residual** (`evidence/replay/wave2_p22_final/`, n=3 on 6409 and 6367, $5.32): **21 turns asserting a negative, 19 explained (90%)**, against **2 of 10 (20%)** on the same sessions in Wave 1; terms named 20%→90%, miss attributed to the index 40%→95%, and **turns blaming the lawyer's own citation 2 → 0** — 6373's *"could you confirm the year or the SI number?"* for a correct citation, where `ssi/2026/170` and `ssi/2025/377` are both verified 404s in LEX. **The residual: both failures are turns with ZERO delegations**, a negative carried forward from an earlier turn, so no search ran and no footer fired. The footer is per-turn; a conversation is not. Left as a limitation — restating full scope on every follow-up would be noise. **State plainly that the mechanical test is now circular:** the footer satisfies the three conditions by construction, so `replay_report negatives` prints a second **model column** graded with the footer stripped. **That column is unstable and is reported as such** — it swung **56% → 4%** between two sweeps the model could not distinguish (the footer is appended after the model has finished), which n=3 cannot produce legitimately. **SIX instrument errors in this row alone, and one is a new failure mode: a product change corrupting the instrument measuring it.** The footer's own words (*"anything reported above as not found was not found in this index"*) trip `NEG_ASSERTED`, so selecting the denominator on the full answer enrolled every researched turn including purely positive ones (23 → 34). The denominator is now taken from the model's prose. The other five were all *under*-reads: a first `NEG_ASSERTED` that scored **61 of 153 turns, 100% failing** by counting *"no winding-up order may be made, except by the company's directors"* as a bare negative; a `terms` rule that demanded "searched" adjacent to "for", then demanded quotation marks; an `index` rule defeated by a 158-character instrument title (**every commencement SSI's title contains a full stop — "Commencement No. 1" — so a sentence window keyed on `.` cannot cross the titles this corpus is about**); and a `limits` condition demanding the model recite an **inert** `year_to=2026` that excluded nothing, now `n/a` where no filter could have bitten. Every correction was re-validated against Wave 1, where the before-side moved only 44/44 → 44/38 failing. **Also corrected here: P5.3's "UK SI 2026 ~0% held"** — false in the direction that matters, since `/legislation/search` returns **27 distinct `uksi/2026/*`** instruments and all 12 spot-checked resolve on lookup. A 60-point census says ~2%. **And the replacement figure was itself falsified the next day** — the same command returned **8%** for SSI 2026 on 16 Sep (1/60 vs 5/60, ordinary noise at n=60). So the product string states **bounds that survive the spread** ("roughly 85%", "under 10%"), never a point estimate and never "none". `lex_probe --coverage` now samples 60 points and **runs** the `/legislation/search` cross-check rather than recommending it, because it was still executing the census that produced the retracted headline. **Opened:** **P3.7** (`/legislation/lookup`, the deterministic held/absent test this row can only disclose around). **Decision most open to being overruled:** the footer is unconditional on any turn that searched, so it appears on positive answers too — gated on a prose detector it would fail silently, which is the trap this work keeps hitting. `AUDIT_TRACE.md` amended: `final_result` now always differs from `raw_result` for both legislation search tools, so a consumer must key on the `summarised` flag, not on the texts being unequal.
| `[x]` | **P2.3** | **B3(b) — Forbid unverified "made under" assertions.** ~~No tool exposes *made under*, *commences*, *amends*, *revokes* or *suspends*~~ — **false for four of the five, established at P5.1 (Session 5): `/amendment/search` exposes amendment, commencement, repeal and revocation, provision-level and directional. Only *made under* has no route.** This row therefore narrows sharply: it is the holding fix for **enabling power alone**, and the other four get a retrieval fix (**P3.5**) rather than a disclosure. The model answers relationship questions from search-result adjacency. In 6340 it listed three real, correctly-cited SIs as made under s.117 Education (Scotland) Act 1962 — **the Act is a 404 in LEX** (`ukpga/1962/47`) and the three SIs are simply the top keyword hits for its title. Link-checking cannot catch this, which is what makes it the worst bucket. ~~Until P5.1 answers whether the relation is retrievable:~~ **P5.1 is answered.** For commencement, amendment, repeal and revocation the rule becomes *retrieve it* (P3.5). For **enabling power** the original rule stands, ~~the model may assert it **only** where a retrieved preamble, provision or `description` states it~~ **narrowed at build (Session 7) from three routes to two: a retrieved preamble or `description`, NOT "a provision"** — a provision states the *class* ("an Order in Council made under this subsection") and never the *instance*, and a screen for it fired on **11 of the 12** unverified turns because generic regulation-making boilerplate is in essentially every enabling Act. Admitting it would license every claim. The model must otherwise say the relationship could not be verified. Enforce in code (invariant 2), not by prompt alone. ~~**Acceptance** *(replay, n=3)*: 6340's correct answer is "this Act is not held in the database, so I cannot list instruments made under it" — any list of SIs is a failure regardless of citation quality. Plus 6409, 6410, 6354.~~ **Acceptance rewritten — see below; 6340 does not reproduce and 6409/6410/6354 assert no derivation at all.** **Evidence:** 6340 6409 6410 6354 6383 6374 6345 6406 6408 6382 6411. **Depends on:** P2.2 | | **DONE (Session 7) — acceptance passed. The row's central fact holds; TWO of the premises handed to it do not, and one of them decides the design.** **(1) The "retrieved preamble" route EXISTS, and the handover said it did not.** The check it rested on — `/legislation/text` for `ssi/2020/295` returns 545 chars of `full_text` opening at "Section 1) Citation and commencement", no recital anywhere — is correct about `full_text` and correct about that instrument, and the conclusion drawn from it is wrong. The recital arrives in **`legislation.description`**, and `get_legislation_text` returns the response **unslimmed**, so the permitted branch is live *today*. **6340 rep 1 exercised it**: the model read `uksi/1979/766`'s preamble verbatim (*"In exercise of the powers conferred upon me by sections 75(c) and 144(5) of the Education (Scotland) Act 1962(a)"*) and reported its enabling powers **correctly**. The one turn in the whole corpus that makes a *supported* derivation claim is in the session this row is written on. **(2) So P3.6 is NOT a prerequisite, and that decision is recorded rather than assumed.** P3.6 would put the same `description` on *search* rows (Phase 1); it does not make the permitted branch reachable, because it already is. It stays its own row. **(3) But the row IS a near-total prohibition, and it says so in the product.** `python -m tools.lex_probe --enabling` (new, 103 instruments, 2026-09-16): **uksi pre-1990 18/25 (72%)** carry a recital; **uksi 1990-2009 0/25, uksi 2010+ 0/25, ssi 1990-2009 0/13, ssi 2010+ 0/15**. **Zero for Scottish instruments in either era** — the corpus these lawyers work in. Modern descriptions state what an instrument *commences*, not what it was *made under*. Over the whole post-Wave-1 replay corpus (**38.8M chars of raw retrieval, 2,504 tool results** — `replay_report --dir <dir> corpus`) only **12 results carried an instrument preamble**, covering **6 distinct instruments, every one in session 6340**. ~~33M chars, 1,907 results, exactly two recitals~~ — **corrected 2026-09-16 when the figures were put behind a command: the counts omitted `wave2_p21`, and the screen that produced "two" required `Act <year>` to follow the phrase, which misses the 1963 *"Whereas the Treasury has determined under section 69(4)"* form.** **Built at the four seams P2.2 established, plus the prompt** (`src/utils/search_scope.py`): (a) `enabling_power_note` on every `get_legislation_text` of an SI/SSI — computed from the RAW result and appended after summarisation, because the summariser drops the preamble first — which either **quotes the recital and permits the claim** or states in terms that this record does not carry one and forbids it; (b) one clause each on the two search blocks, gated on the rows actually containing secondary legislation, naming 6340's adjacency inference and forbidding it; (c) an enabling-power limb on `worker_scope_block`, because the Manager and the DR synthesis never see a tool result; (d) one clause on `answer_scope_footer`, gated on the **structural** fact that the turn retrieved an instrument — never on a prose detector, which fails silently; (e) `_ENABLING_POWER_RULE`, one shared string on all three legislation worker prompts. **The strip needed widening and that was a live defect**: `[ENABLING POWER …]` is not `[SEARCH SCOPE …]`, so without the change the block would have rendered to a lawyer. **Acceptance, rewritten on evidence that still reproduces** *(replay, n=3, graded per TURN with `replay_report --dir <dir> derivations`)*: ~~6340/6409/6410/6354~~ — **6340 no longer fabricates the SI list** (baseline records it as `N (n=3)`, the same trap P2.1 hit with 6406) and **6409/6410/6354 assert no derivation at all**; grading them would measure nothing. The live evidence is **6383** (7 claim-turns across `wave1` and `wave2_p21`, including the corpus's largest: *"Over 130 instruments explicitly cite section 95 in their preamble"* — a claim about a document part no endpoint returns), **6382** (2) and **6374** (1). 6340 is kept in the sweep for the opposite reason: it is the **only** session that exercises the permitted branch, and a fix that suppresses a claim the material supports is a regression. **Before-column, measured over the post-Wave-1 corpus (219 answered turns): 13 turns assert a derivation (20 claims); 12 turns / 18 claims had NO enabling-power text retrieved — 92%.** `baseline/` is 0 of 222, which is not an improvement: pre-Wave-1 the jurisdiction filter retrieved nothing to derive from, so **B3(b) is a defect surface Wave 1 opened**. **The detector was an artefact twice before it was right, and both are recorded in `replay_report.py`:** draft 1 scored **69 of 155 turns** on a regex alternation that reduced to a bare `\bis`, plus an instrument screen the bare word "regulations" satisfied; draft 2 over-corrected to **1 turn** by compiling that screen case-sensitively and so missing every claim opening "Several SSIs". Validated in **both** directions — `derivations --drops` prints every sentence in the same vocabulary that was not counted, and all of them were read. The one that matters most is 6383 t2's *"the agent could not retrieve the preamble to definitively confirm if it was made under section 95"*, which is the **right** answer and must never be graded as a defect. **Known under-read, stated rather than patched:** an anaphoric subject (*"It is made under powers including section 95"*, 6383 t1) is not counted; adding `it` as an instrument reference would be unboundedly over-broad. It under-reads before and after equally. **ACCEPTANCE PASSED** (`evidence/replay/wave2_p23/`, n=3 on 6340, 6374, 6382 and 6383 — 12 runs, **$7.10**, 1 h 5 m, zero model mismatches). Graded per TURN: **turns asserting an unverified derivation fell from 12 of 28 (43%) to 2 of 29 (7%)**, unverified claims 18 → 3, and **both survivors carry the code-emitted line saying so, where none of the twelve before them did**. **The two sessions that carried the bucket went to zero**: 6383 (7 of the 13 before-column claim-turns, 12 turns over three reps) and 6382 assert nothing unverified in any rep. 6383 turn 4 went from *"Over 130 instruments explicitly cite section 95 in their preamble"* to *"the retrieved texts lack enabling power recitals"*. **Invariant 1 held in both directions, which was the real risk.** Answers did not shrink to buy the number — per turn they GREW in 8 of the 10 turn slots, 6383's conversational turns roughly doubling (548→1,377, 604→1,380, 614→1,570 chars) because the model now explains the gap instead of asserting across it; 6382 still delivers the substantive finding it was asked for; and **6340, the only session that exercises the permitted branch, asserts SUPPORTED derivations in all three reps**, now phrased *"explicitly states it was made under…"*. **The after-column was audited in both directions too** — `derivations --drops` prints 72 uncounted sentences and all were read; all are correct drops, overwhelmingly explicit *"could not be verified"* statements. **Two residuals, both 6374, both the mild shape**: class-level claims (*"Orders in Council made under section 126(8)(b)"*) where the parent Act's s.126(8) does say offices are "specified in an Order in Council made under this subsection" — the instance-level link is inferred, the verdict is right, and the disclosure fires. **One measured gap, left as a limitation**: the lawyer-facing clause is gated on the turn having retrieved an instrument, which covers **12 of 13** before-column claim-turns; a turn naming instruments from search rows alone gets no clause, and closing that would fire it on nearly every legislation turn for an 8% gain. **A P2.2 defect this sweep exposed and fixed**: `answer_scope_footer` stripped only the OUTER quotes, so a query rendered to the lawyer as `"Education (Scotland) Act 1962" 117"` — unbalanced, reading as two searches where there was one. **764 tests** (712 → 764). Full numbers: `BASELINE.md`, *B3(b)*. **ONE GAP IN THIS ROW'S DETECTOR, found at P2.5 (Session 9) and NOT closed there.** `derivation_claims` requires a **named instrument** plus a derivation predicate, which is what keeps it off ordinary citations. It therefore misses a derivation claimed for a **class** of instruments: 6411 rep 1 wrote *"we would need to retrieve the specific commencement orders made under section 130 of the Act"* and `derivations` read 0 for that turn. s. 130 **is** the Scotland Act 1998's commencement power, so the claim is true - and it was not retrieved, which is exactly what this row forbids. The fix is not obvious: loosening the named-instrument requirement is what would make the detector fire on ordinary prose, and this row's acceptance numbers are stated against the strict version. **Measure how common the class form is before changing anything**, and note that P2.3's published "2 of 29" describes the strict detector.
| `[ ]` | **P2.4** | **B12 — Deterministic disclosure of the case-law corpus gap.** The National Archives corpus does not index the Court of Session or the Sheriff Courts. AILA discloses this well in 6341, 6385 and 6407 — and **not at all in 6375, where it mattered most** and produced a substantive error (English common interest privilege analysed as Scots law). Inconsistent disclosure is worse than none: users calibrate to the warning and are then not warned. Add a footer on any Scots-law case-law answer, in code. Separately, distinguish "absent from the index" from "does not exist" in the wording — in 6373 AILA questioned FrankieH's citation of SSI 2026/170 when the citation was right and the index was short. **Acceptance** *(replay, n=3)*: 6375 carries the disclosure; 6373 attributes the miss to the index, not to the lawyer. **Evidence:** 6373 6385 6363 6375 6340 6359 6409. **Depends on:** P2.2 | **Sharpened by P5.2's probe (Session 5), and the disclosure now has a stronger and more precise line to take.** The gap is **total, not thin** — `court=csoh`/`csih` are rejected by TNA as invalid court codes — and a Scots-law query returns **50 English judgments that merely mention Scotland**, not nothing. So the footer should not say "may be incomplete" — it can be precise. **The exact wording matters and the obvious version is wrong.** TNA offers **42 court codes, none Scottish and none Northern Irish** (scraped from the search facet, Session 5) — but **Scottish appeals that reached the UK Supreme Court ARE indexed**: *Daly v HM Advocate (Scotland)*, *ABC v Principal Reporter (Scotland)* and *X v Lord Advocate* all return from `court=uksc`. So "Scottish courts are not indexed" is **false**, understates what is available, and would make a lawyer distrust a result that is actually sound. The true and useful line is: **Scottish appeals to the UK Supreme Court are indexed; the Court of Session, Sheriff Appeal Court, Sheriff Courts and High Court of Justiciary are not** — which also tells the lawyer where the one real Scottish route is, rather than only warning them off. Note the UI's Court filter offers 14 courts, **none Scottish** — a user-visible statement of the same fact that the answer should not contradict by silence.
| `[x]` | **P2.5** | **B4 (model half) — Stop asserting in-force status.** Pairs with P1.2. ~~There is no in-force signal in the tool surface, yet the model states currency confidently~~ — **corrected 2026-09-15 during P1.5: a prompt instructs it, and the report structure compels it.** This is the third instance of the pattern that explains two of Wave 1's four rows: *before treating a defect as model behaviour, check whether a prompt instructs it.* Three sites tell the Worker to state currency — `prompts.py:111` (*"note if the law applies to the UK, Scotland, or E&W, and if the legislation is in force"*), `prompts.py:236` (*"whether legislation is in force"*) and `prompts.py:1060` — while the only metadata the model has is LEX's `status`, whose vocabulary is `final`/`revised` and means *which text version is held*. Hence the signature in the data: of 34 matches across the baseline, almost every one reads *"is currently in force (status: revised)"* — the model is faithfully reporting the field it was pointed at. **Consequence for scope: P1.2 reduces B4 but cannot clear it**, because the instruction to state in-force status survives the filter's removal — early Wave 1 evidence, 6341 went 3 to 1, not to 0. **And the obvious fix is a trap:** "Jurisdiction & Status" is a *mandatory* section in `_REPORT_SECTIONS` (`agent_core.py:51`), so telling the model to omit it makes `_report_needs_reformat` judge the report malformed and spends an A4 reformat call re-adding the heading — which the model then fills with the same conflation. The fix must change what the Status line is *permitted to say* (explicitly: that in-force status is not determinable from the available sources, and that `status: revised` is not evidence of it), not remove the section. Original evidence: in 6341 it said every provision cited was in force (ss.38–39 Shops Act are repealed) and then **invented an explanation** for the missing text. Remove the claim until there is a source for it. **Acceptance** *(replay, n=3)*: 6341 and 6411 make no in-force claim unsupported by a retrieved commencement source. **Evidence:** 6341 6411 6378. **Depends on:** P1.2, P2.2 **RE-CHECKED AT THE END OF SESSION 8 (2026-09-16), because P3.5 changed this row's premise and because its line references had gone stale. Nothing below needs re-deriving.** **(1) The three prompt sites have MOVED — P3.5 inserted text above all of them.** They are now `prompts.py:155` (`WORKER_SYSTEM_PROMPT`, *"note if the law applies to the UK, Scotland, or E&W, and if the legislation is in force"*), `prompts.py:282` (`WORKER_SYSTEM_PROMPT_HYBRID`, *"whether legislation is in force"*) and `prompts.py:1096` (`DEEP_RESEARCH_SYNTHESIS_PROMPT`, *"Territorial extent and in-force status where the findings report them"*). ~~111, 236, 1060~~. `_REPORT_SECTIONS` is now `agent_core.py:60`, not `:51`. **`WORKER_SYSTEM_PROMPT_CONVERSATIONAL` carries no in-force instruction at all** — do not assume the symmetry P2.3 and P3.5 have, which is three prompts; this row is three *different* sites, two of them worker prompts and one the DR synthesis. **(2) P3.5 added two in-force instructions of its own and they must not be contradicted.** `prompts.py:102` and `:132` now tell the Worker that *"if the question asks whether something is in force … you MUST call `get_legislation_changes` before answering"*. That is the route; this row governs what the **Status line may say when the route comes back empty**. A fix that forbids in-force statements outright would fight P3.5 and suppress answers that are now properly sourced. **(3) THE FINDING THAT MOST CHANGES THIS ROW: for BOTH of its acceptance sessions the retrieval route comes back EMPTY, so P2.5 remains almost entirely the negative branch.** `ukpga/1998/46` (6411's Scotland Act 1998) returns **857 relations and ZERO `coming into force` rows** — the Act's commencement predates what the change record carries. And 6341's Shops Act 1950 is **not held at all**: `ukpga/1950/28` is a 404 on `/legislation/lookup`, because pre-1963 Acts use regnal ids (`ukpga/Geo6/14/28`), so there is no change record to consult. **So do not design this row around "route to `get_legislation_changes` and report what it says"** — that is right for modern Acts and does nothing for either acceptance session. The bar in the acceptance line — *no in-force claim unsupported by a retrieved commencement source* — is unchanged and is now met by suppression, not by retrieval, on exactly these two. **(4) Commencement coverage is uneven by era and the boundary is NOT established.** Sampled 11 Acts (`/amendment/search`, `search_amended=True`): commencement rows present for 2005, 2009, 2010, 2014, 2018 and 2025 (`asp/2014/18` 258 of 309, `ukpga/2010/15` 647 of 1,245, `asp/2018/9` 184 of 484) and **absent for 1998, 2000 and 2002** (`ukpga/1998/46`, `asp/2000/1`, `asp/2002/3` — 857, 97 and 158 relations, none of them a commencement). **Eleven points is not a cut-off**: the standing rule is that a sparse sample proves nothing about a corpus, and this one is sparse. It is enough to know the route fails for 6411 and to know the Status line must handle both cases. Widen it with `python -m tools.lex_probe --commencement`, whose duplicate/size sections already walk a sample. **(5) One observation worth a measurement, not a design**: `ukpga/1965/35` comes back from `search_legislation` titled *"Shops (Early Closing Days) Act 1965 (repealed 1.12.1994)"* — for some instruments the repeal, **with its date**, is carried in the title string, which `_slim_search_results` keeps and the model already sees. That is a possible cheap partial source for a Status line. **Seen once. Measure how common it is before building on it** — count titles matching `\(repealed [^)]*\)` across the `search_legislation` rows in the replay corpus (`replay_report --dir <dir> corpus` already walks them) before treating it as a route. **(6) Before-column, already captured**: 6411 is in `baseline/` and `wave1/` and says *"Yes, the Scotland Act 1998 is in force"*, naming *"the Scotland Act 1998 (Commencement) Order 1998"* and a date — neither retrieved in that turn. 6341 is in both directories too, is an 8-turn session with Deep Research on turn 7, and is the one that said every provision cited was in force and then invented an explanation for the missing text. **BUILT (Session 9, 2026-09-16); acceptance sweep pending at the time of this commit.** **The row's premise held and THREE of the six facts written into it at the end of Session 8 did not survive measurement — one of them changes the design and one of them is a live defect P3.5 left behind.** **(1) `ukpga/1998/46` does NOT have "857 relations and zero commencement rows".** It has **29 `Commencement Order` relations**, and the reason Session 8 read zero is that its probe filtered `type_of_effect == "coming into force"` exactly. **But the handover's conclusion survives, for a better reason than it gave, and the difference is a trap:** across the **1,355** `Commencement Order` relations the corpus's legislation_ids produce, the `changed_provision` is one of **eight placeholder values** (`specified amended provision(s)` 1,068, `None` 199, `C/O` 73, `specified provision(s)` 11, plus four casing/typo variants and one `Act`) and **never a provision of the subject** — against 5,180 distinct real provisions over 19,031 `coming into force` rows. Those rows mean *another Act's commencement order brought into force an amendment TO the subject*: `ssi/2001/81`, the Adults with Incapacity (Scotland) Act 2000 (Commencement No. 1) Order 2001, appears against `ukpga/1963/41` because `asp/2000/4` substituted words in its s. 90(1). `asp/2000/4` carries both classes (35 real, 14 placeholder), so the split is not academic. **P3.5's block invites the model to state any relation the record lists, citing the instrument named against it** — so left merged, the fix for 6411's unsourced *"the Scotland Act 1998 (Commencement) Order 1998"* would have been a differently-sourced WRONG answer. `_slim_amendment_results` now counts the three classes separately (`provisions_commenced`, `commencement_orders_of_amendments`, `repeal_or_revocation_relations`), marks those groups `commences_this_legislation: false`, sorts them to the back with the self-referential ones so they cannot displace a real relation at `_MAX_RELATED_INSTRUMENTS`, and the block forbids naming them. **(2) The `status` vocabulary is THREE values, not two: `final` 60.3%, `revised` 38.8%, `stub` 0.9%** over the 15,160 model-visible search rows in all seven replay directories. Conclusion unchanged and slightly stronger — none of the three is an in-force signal — but P1.2's row and `test_current_only_removed.py`'s docstring both stated the wrong vocabulary and are corrected. **(3) The row's unmeasured lead (5) resolves as a FLAG, not a date route.** A repeal/revocation marker in the title appears on **258 of 15,160 rows (1.7%), 43 distinct titles**, and a date follows the keyword on only **8 of the 258**. It is worth reading and it is **asymmetric**: present means the instrument is not in force, absent means nothing at all — which is exactly the shape a Status line needs, and which the first draft of this row's own detector got wrong in the flattering direction (it graded 6411's *"Yes, the Scotland Act 1998 is in force"* as SOURCED because an unrelated repeal-marked row appeared in the same turn's search results). **(4) And a fourth lead, chased and closed: the effect STRING sometimes carries a date** — `saved (6.5.1999)`, `amended (1.7.1999)`, `repealed (1.1.1996)` — so P3.5's flat "there is no DATE on any relation" looked wrong. Measured over all 272 corpus legislation_ids in both directions: **552 of 89,465 relations (0.6%) embed a date, and ZERO of the 19,031 `coming into force` rows do.** P3.5's statement stands exactly where it matters, the second hop is still required, and the next session does not need to re-chase this. **THE FIX, at the four seams `src/utils/search_scope.py` owns, plus the slimmer and the prompts:** **(a) the affordance is removed, not argued with** — `_slim_search_results` emits `status` as **`text_version`**. Of `wave1`'s 65 in-force assertion sentences, **48 cite the text version as the evidence** (*"is currently in force (revised)"*, *"Status: Revised (In force)"*); the model was not inventing a source, it was quoting the only field that looked like one. `extract_sources` reads the new key with a `status` fallback, so the Sources rail is byte-identical. **(b) one sentence on the search block** — kept to one because it rides on 790 searches a sweep and `test_a_long_query_is_capped_not_dropped` bounds the block at 2,000 chars (the first draft broke it). **(c) `currency_note`** — a `[CURRENCY …]` block naming the repeal-marked titles on the 1.7% of pages that have one, and silent on the rest. **(d) `record_currency` + `_currency_limb`** on the worker's report block, which is **the one limb here that speaks when its step found nothing** — the opposite gate from `_enabling_limb` and `_relations_limb`, because a step that established nothing about currency is exactly the step whose report says *"all cited legislation is currently in force"*, and the Manager writing that sentence has seen no tool result. **(e) `_currency_footer_clause`** on the lawyer-facing footer, gated on the structural fact rather than a prose detector, in both branches. **(f) the strip widened to `CURRENCY`**, test written first — the live defect P2.3 hit with `[ENABLING POWER …]`. **(g) `_IN_FORCE_RULE` on all THREE legislation worker prompts and a rewritten Status line at all three sites.** The row warned against assuming P2.3's and P3.5's three-prompt symmetry; that was right about the Status *lines* (three different sites, two worker prompts and the DR synthesis) and wrong about the *rule*: **`WORKER_SYSTEM_PROMPT_CONVERSATIONAL` had no in-force instruction, and `get_worker_system_prompt` returns it whenever `_chat_mode == "conversational"` — which is the mode 6411 ran in.** The site with no instruction was the site with the defect. **The mandatory section is kept** (the A4 trap the row flagged) and what it may *say* changed: territorial extent from the metadata; currency only from a retrieved `coming into force` or repeal relation, a title marker, or `valid_date`; otherwise "not verified". **`valid_date` on `/legislation/text` is the substitution that keeps this on the right side of Invariant 1** — `2026-03-11` for the Scotland Act 1998, the date the held revised text is up to date to, available on the 18% of turns that reach Phase 3 and rising under P3.5. Not an in-force date, and the honest thing a Status line can say instead of nothing. **THE INSTRUMENT, and the old one is deliberately left alone.** `replay_report currency` grades per turn on one prose test against one structural fact read off the audit trace, so the headline cannot be moved by a wording change in the product (P2.2's denominator was). **`IN_FORCE_CLAIM` — which produced the published 27 -> 30 series — is byte-identical, and it undercounts by 30%**: it requires "is/are/remains/currently in force" and so catches none of the bare Status bullets that are the purest form of the defect (`In force (revised).` 6341 x3, 6384 x6, 6389 x3; `Status: Revised (In force).` 6406 x4). Properly counted `wave1` is **43 turns / 65 assertions**, not 30 / 34. Both are printed. **`SOURCED` beside `UNSUPPORTED` is the suppression check and must not reach zero** — this row's specific risk is the inverse of P3.5's. Validated in both directions on all 86 `in force` sentences in `wave1` (37 pinned strings in `tests/test_in_force_status.py`), and re-run over all seven historical directories: `baseline/` 54 unsupported, `wave1/` 43, `wave2_p21/` 7, `wave2_p22/` 4, `wave2_p22_final/` 5, `wave2_p23/` 10, and **`wave3_p35/` 0 unsupported with 4 sourced** — P3.5's own sessions already show the shape this row wants where its route works, and 2 of those 4 still cite a text version (*"SSI 2020/475 is in force (status: revised)"*), which is the residual this row removes. `python -m tools.lex_probe --inforce [--full]` reprints every figure above. **922 tests** (833 -> 922). | **ACCEPTANCE PASSED** (`evidence/replay/wave2_p25/`, 8 runs - 6341 and 6411 at n=3, plus **6409 and 6383 at n=1 which are in the sweep solely for the suppression check**; 42 answered turns, **$9.76**, ~2 h, zero model mismatches - and `evidence/replay/wave2_p25b/` for 6411 re-run on a strengthened prompt, $0.90). Graded per TURN with `replay_report currency`. **Turns asserting currency with NO commencement relation retrieved went 7 of 24 to 0 of 42; assertions citing a text version as the evidence 13 to 0.** **The suppression check is the number this row could most easily have faked, and P3.5's win survived intact**: the commencement relation was still delivered in **6 of 6** in-scope turns (`wave1` 0 of 8), with zero denials - 6341 asks definition questions and 6411's Act has no `coming into force` relation at all, so `sourced` is legitimately 0 on both and a fix that silenced the answers the material supports would have looked identical on those two alone. **Invariant 1: 18 of 24 matched turn slots grew**, tool calls per answered turn 15.5 to 15.2; the six that shrank were each read and **none is currency suppression** - a declined duplicate question that correctly redirected on mode, the blank report below, one turn that is **shorter and strictly better** (6409 t7: a false negative replaced by the right answer, which is P2.4's failure removed), and three clarifying questions that `wave3_p35` asked identically before this row. **Stated costs, not buried:** **10 of 30** turns whose question never mentioned currency carry a disclaimer anyway (33%, `currency --unasked`; ~~3 of 8 in one rep~~ was quoted before the measure had a command behind it), while only 1 of the 11 that DID ask carries one because the rest got a sourced answer (the price of `_currency_limb` speaking unconditionally, and the right trade given the section is mandatory and previously said *"All referenced legislation is currently in force"* about a session citing an Act whose ss. 38-39 are repealed); the limb costs 873 characters on every legislation worker report; on 6341 rep 1 cost went $2.09 to $2.24 (+7%) while tool calls fell 198 to 180 and the mean answer grew 4,205 to 6,245 chars. **A second directory was needed and the reason is recorded**: 6411 obeyed the *report what the record holds* clause in **1 of 3** reps, so one line was added to the conversational PHASE 2b and 6411 re-run - **2 of 3 reporting the repeals, mean 741 chars against `wave1`'s 480**, and rep 1 retrieved the actual Commencement Order 1998. **THE INSTRUMENT WAS WRONG EIGHT TIMES, in both directions**, five of them found by reading the after-column of a live run where the model wrote exactly what the product now asks for; the last four all read the FIXED behaviour as the defect, so the after-column ran pessimistic rather than flattering. Full list in `SESSION_LOG.md` Session 9. After every correction, re-run over all seven historical directories: **not one before-column number moved.** **957 tests** (833 -> 957). Full numbers: `BASELINE.md`, *B4*.
| `[x]` | **P2.6** | **The A4 reformat retry fires on halted workers.** *(New row, from P2.1's baseline replay — not scope creep inside P2.1: different code site, different cost, and it would survive a complete fix to the halt presentation.)* In 6340 the worker halted with no findings, `_report_needs_reformat` saw a report with no section headers, and `_reformat_worker_report` spent a no-tools LLM call producing a perfectly-structured empty report — *"Jurisdiction & Status: Not applicable (no research generated). References: None found."* A halted worker has nothing to reformat, so the retry is pure cost and it **launders a failure into something that looks like a finished report**, which is what lets the halt read as a legal answer downstream. Short-circuit: skip the reformat when the worker report is a halt (or is empty), and count it. **Note it also pollutes the P1.5/Wave-3 measurement** — `report_reformat_retries` and the Efficiency tab's `reformat_rate` currently score these as prompt-adherence failures when the model never had a report to format. **Acceptance** *(deterministic)*: unit test — a halt-only worker report produces no reformat call and increments no `report_reformat_retries`. **Evidence:** 6340; re-count across the baseline. **Depends on:** P2.1 | **DONE (Session 5).** One-line guard in `run_worker_agent`, keyed on P2.1's new `result["halted"]` — which is why this row depended on P2.1 rather than duplicating its detection. Deliberately narrow: an unhalted malformed report still gets its retry, and there is a test for that, because disabling A4 wholesale would trade one defect for another. **Acceptance passed** (`tests/test_research_halt.py`): a halted worker makes exactly one LLM call and increments no `report_reformat_retries`; an unhalted malformed one still makes two and increments one. Note the correctness half was already closed by P2.1's overwrite — this row is the wasted call and the polluted `reformat_rate`.
| `[ ]` | **P2.7** | **The legislation bot has no discovery budget, and that is what the step cap is catching.** *(New row, from P2.1's cap decision — a different mechanism from P2.1 and it survives a complete fix to the halt presentation.)* `run_worker_agent` creates `search_budget = {"remaining": 3}` **only** for `parliamentary_records` and `westminster_records`; for every legislation mode it is `None`, so nothing stops a Worker looping on `search_legislation`. Measured over the completed Wave 1 sweep, the 11 turns that hit the 20-round cap carry **26% redundant tool calls against a 15% base rate** — they are looping, not starved. Two shapes, both in the data: **repeat retrieval** (6335 turn 7: 22 of 26 calls redundant; 6382: 24 of 53; 6338: 19 of 27) and **discovery flail** (6409 turn 6: **22 Phase-1 searches, 1 Phase-2 retrieval**; turn 7: **25 searches, 0 retrievals** — zero redundancy because every query was different, and zero retrieval because it never stopped searching). The parliamentary budget exists for exactly the second shape and the legislation bot never got one. **Note the interaction with D5's tool memo:** an exact-arg repeat is already free (served from the memo) and still counted redundant, so the 26% is loop *behaviour*, not spend — the cost is the ReAct rounds it burns, which is what pushes a run into the cap. **Do NOT simply copy `{"remaining": 3}`** — the legislation Phase-1/Phase-2 shape is different from Holyrood's and the right number must come from the distribution (median Phase-1 calls per turn, not a guess). **Acceptance** *(deterministic + replay)*: a unit test that the budget hard-stops discovery and does not touch Phase-2 retrieval, plus `react_turns_max` at-cap falling on a re-sweep without `sources_kept` falling. **Evidence:** 6409 6335 6382 6338. **Depends on:** P2.1 | **ONE MORE INSTANCE FOR THIS FAMILY, found at P2.5 (Session 9) and deliberately not given its own row.** 6341 turn 8 asks *"what do you mean when you say legislation is noted as a stub"* - a meta-question about the index - and the answer explains stub records, which instruments tend to be stubs, and what to consult instead. None of it retrieved, all of it plausible, and one clause (*"Statutory Instruments that were revoked or superseded before the database was comprehensively populated"*) is a guess about LEX's coverage stated as fact. It is not B4 (no currency claim), not B5 (no negative asserted) and not B3(b), so it had no owner; it is the same shape as the speculation `section_search_note` exists for (6335 turn 7's *"the database is having difficulty parsing the Schedule B1 structure"*). **One turn is not a rate** - measure it over the corpus before extending this row to cover it.
| `[ ]` | **P2.8** | **A negative carried forward from an earlier turn is never re-qualified.** *(New row from P2.2's acceptance, Session 6, per the re-planning protocol — it is a measured, reproducible residual with a known mechanism, not a note.)* P2.2's scope footer is emitted per TURN, from the searches that turn ran. **A conversation is not per-turn.** Both of the two failures in P2.2's acceptance sweep are turns with **zero delegations**: the Manager answered from the conversation history and restated an earlier turn's negative — *"As noted in the previous search, The Social Security (Amendment) (Scotland) Act 2025 (Commencement No. 2) Regulations 2025 (SSI 2025/377) is not currently available in the legislation database"* (6409 rep 1 turn 11) and *"no commencement regulations have been found to indicate this has happened yet"* (6409 rep 3 turn 4). No search ran, so no footer fired, and the qualification the lawyer saw two turns ago is not repeated beside the claim they are reading now. **2 of 21 negative turns (10%) in the acceptance sweep**, and the rate will be higher in real multi-turn use than in a replay, because a lawyer follows up more than a script does. **Do NOT fix it by emitting the footer on every turn regardless** — a footer describing searches that did not happen is a worse lie than silence. The candidates are (a) carry the previous turn's search scope on the request config and re-state it in short form when the answer repeats a negative, or (b) make the Manager re-delegate rather than answer a factual negative from memory, which overlaps **P2.7**'s budget work and would cost a delegation per follow-up. **(b) is probably wrong**: re-running a search to restate a known negative is exactly the redundancy P2.7 exists to remove. **Acceptance** *(replay, n=3)*: on 6409, a follow-up turn restating an earlier negative carries the qualification, and no turn carries a scope statement describing a search it did not run. **Evidence:** 6409 (rep 1 turn 11, rep 3 turn 4). **Depends on:** P2.2 |
| `[x]` | **P2.9** | **A memo-served search is missing from the scope record P2.2 emits.** *(New row from P2.3, Session 7. A defect in a shipped fix, found while wiring P2.3's record alongside it.)* `run_worker_tool` returns early on a **tool-memo hit** and that path does not call `record_search` (`agent_shared.py`, the `if memo_key is not None and memo_key in tool_memo:` branch). The memo is **per-request**; `search_log` is **per-worker-run**. So when a Deep Research step repeats a search an earlier step already made, the second step is served from the memo and that query **never enters its own run's record** — it is therefore absent from `worker_scope_block` (what the Manager and the DR synthesis are told was searched) and from `answer_scope_footer` (what the lawyer is told was searched), and the footer's *"searched the legislation index N time(s)"* count is short. **This under-states a disclosure whose entire purpose is to be complete.** ~~**Measured over `wave1` + `wave2_p21` + `wave2_p22_final` + `wave2_p23`:** 23% of `search_legislation` calls are memo hits (359 of 1,497), and **78 of 358 worker runs (22%) lose at least one query from their own record — 150 distinct queries across 47 turns.**~~ **Restated at the build (Session 11), because the denominator included two directories where the scope block did not yet exist.** `wave1` and `wave2_p21` predate P2.2 and carry **no block at all** (182 and 56 runs), so they cannot lose anything and must be excluded, not counted. Over the **six** directories that do have a block: **277 of 1,189 searches (23%) absent from the record, in 86 of 259 worker runs (33%)** — and `issued - recorded == memo hits` **exactly, per run, with no exceptions across all six**, which is what identifies the memo as the sole cause rather than one cause among several. Command: `python -m tools.replay_report --dir <dir> scoperecord`. The fix is one line, and the reason it was not taken in P2.3 is that it **moves P2.2's published footer contents**, so it needs its own before/after rather than riding on another row's. **Note the correct shape**: extract the sources / harvest the URLs / record the search on the memo path exactly as the non-memo path does — the memo saves the API call, not the provenance, which is already the stated rule for `_extract_sources_from_tool` and `harvest_legislation_urls` two lines above. P2.3's `record_enabling_power` is already called on both paths, deliberately, so this row is only about `record_search`. **Acceptance** *(deterministic)*: a unit test that a memo-hit search lands in `search_log`, plus a re-run of `replay_report --dir <dir> negatives` over `wave2_p22_final` confirming the explained-negative rate does not fall. **Evidence:** the four replay directories above. **Depends on:** — | | **DONE (Session 11). The defect is worse than a short count, and the worst case is in the session this plan cites for honest failure.** **11 of the 86 lossy runs made EVERY one of their searches via the memo**, so their block carries no searched-for line at all — while still instructing the agent writing the negative that it *"MUST quote the search terms above"*. There were none above: **the disclosure contradicted itself.** 6374 rep 1 turn 2 is the measured case, and 6374 is the session CambeulW scored 5/5 *because* AILA said it could not find something (Invariant 1's headline example). **Built** exactly as the row specified — `record_search` on the memo path, gated on the tool name because `record_search` does not self-gate (the two call sites on the non-memo path do it, and this is the third). **Acceptance PASSED**, both directions: five tests, of which **three fail without the fix** and two guard against it over-reaching (a memoised `get_legislation_text` must not be logged as a search of the index; the non-memo path must still record exactly once, since this adds a third call site and a double-count would overstate the footer in the opposite direction). `replay_report negatives` re-run over all six post-P2.2 directories: unchanged, as it must be — the fix touches product code, not the detector. **After-column is by construction, not by sweep:** the record is now complete, so `scoperecord` exits 0 on any directory produced after this commit; no such directory exists yet, so the next sweep any row runs is the first observation. **1009 tests** (998 → 1009). **One instrument correction of my own**, caught by putting the number behind a command: the first detector inferred block-absence from `recorded == 0`, which cannot tell a pre-P2.2 run from an all-memo one, and reported `wave1` as 13 runs "with a scope block" losing 100% of their searches. Keying on the block MARKER settles it, and is pinned by a test.
| `[ ]` | **P2.10** | **A negative asserted by a turn that ran NO tool at all gets no disclosure.** *(New row from P2.5, Session 9. Observed once; measure before building.)* `answer_scope_footer` returns `""` when the turn made no `search_legislation` call, which is right for a turn that says nothing about the statute book and wrong for one that asserts a negative anyway. **6341 rep 2 turn 2** (P2.5's acceptance sweep) declined to re-research a near-duplicate question and wrote *"the initial search already checked the available database and found no case law results"* - a negative, attributed to a search made in a PREVIOUS turn, with no terms, no filters and no footer, because this turn searched nothing. `worker_scope_block` is silent for the same reason (`if not log: return ""`). **This is the first instance in 209 answered turns across five replay directories** (`wave1`, `wave2_p22_final`, `wave2_p23`, `wave3_p35`, `wave2_p25`; the zero-tool-call check is in `SESSION_LOG.md` Session 9), so it is **n=1 and may be stochastic** - the same turn answered normally in reps 1 and 3. **Do not build for it before measuring it.** If it is real the fix is small - carry the previous turn's scope forward, or fire the footer on a turn that asserts a negative having searched nothing - but the second option is a prose detector in the product, which `search_scope.py` refuses for the reason recorded there. **Acceptance** *(deterministic + replay)*: a measured rate over the corpus first, then a unit test pinning whichever fix is chosen. **Evidence:** 6341. **Depends on:** - |

### Wave 3 — Retrieval depth and reasoning (rate-measured; needs a settled baseline)

| | ID | Row |
|---|---|---|
| `[ ]` | **P3.1** | **B10 — Right Act, wrong depth.** 6 primary / 10 total. First, **verify a claim in our own docstring**: `_slim_search_results` says the raw search response carries "a ranked sections array that the model never uses". If real, Phase 2 is discarding the API's own statement of which sections matched and re-guessing in free text — the cheapest precision win available. **Confirm the live shape before building on it**; an unverified assumption about a response field is what made A2 dead on live data. Then allow subsection-level citation (6365: "it only ever cited full sections"), and relax the one-call-per-instrument rule where the question is scoped to a single Act. **Acceptance** *(replay, n=3)*: 6396 (right enactment, provisions not found), 6365, 6348 return the provision at the granularity asked for. **Evidence:** 6396 6365 6348 6369 6333 6380 6354 6347 6372 6385. **Depends on:** P1.5, P2.* |
| `[ ]` | **P3.2** | **B6 — Stop capitulating under challenge.** Pushback is currently treated as correction, so the answer tracks who pushed hardest. 6406 is the demonstration: AILA asserts fish oil is *not* a rendered fat and defends it over three turns, then reverses completely on pushback with a fresh citation — **having already reversed once earlier in the same session**. Both positions asserted with equal confidence and equal apparent grounding. On challenge: re-retrieve before re-answering, and require the revised answer to cite the provision that changed the conclusion. Remove agreement openers ("You are absolutely correct") from the register — they signal a resolution that has not happened. **Acceptance:** needs an **adversarial multi-turn rig that does not exist yet** — build it as part of this row. Replay 6406 turns 11–24 as a scripted challenge sequence; assert the position is either held with the same citation or changed with a new one, and never flips twice. **Evidence:** 6406 6345. **Depends on:** P3.1 |
| `[ ]` | **P3.3** | **B11 — Separate retrieval from interpretation.** Three sessions, all verified wrong or unhedged. 6338 applied the Interpretation and Legislative Reform (Scotland) Act 2010 to a 2009 Act, twice, unhedged — **ILRA s.1(1)(a) applies Part 1 only to Acts whose Bills received Royal Assent on or after Part 1 commenced** (verified at source); HeatherE was right and the correct instrument is the Scotland Act 1998 interpretation order. It was also wrong that ss.35A and 35ZA cannot run concurrently, reasoning from structure rather than text. Encode the distinction between "s.25 provides that…" (retrieval) and "on one reading, X" (advice) and hedge only the second. **Note the counter-pressure**: these users reward decisiveness when it is grounded, and CambeulW asked for neutrality only "for aspects which require a legal analysis" — a blanket caveat is a regression. Add an explicit rule that a doctrine is not asserted for a jurisdiction unless a source for that jurisdiction was retrieved, which also closes 6375. **Acceptance** *(replay, n=3)*: 6338 has a verifiable right answer, so it is a true accuracy test not a style check. Plus 6375, 6370. **Evidence:** 6338 6375 6370. **Depends on:** P3.1 |
| `[ ]` | **P3.4** | **B9 — Default jurisdiction.** Every user of this deployment is a Scottish Government lawyer, and there is no default-jurisdiction rule. 6378 asked about "the UK" and got an England-only answer with no flag; 6360 got the E&W 2007 Rules and reached the Scottish ones only on follow-up. Where the question names no jurisdiction: answer for Scotland and say so. Where it says "the UK": answer for all four and flag divergence rather than silently picking one. **Depends on P1.1** — the filter is currently the only lever a user has and it does not work. **Acceptance** *(replay, n=3)*: 6378 turn 1 and 6360 turn 1 name their jurisdiction explicitly. **Evidence:** 6360 6378. **Depends on:** P1.1, P3.1 |
| `[x]` | **P3.5** | **B3 — Retrieve the relationship instead of forbidding the claim.** *(New row from P5.1, Session 5. The largest bucket turns out to be buildable.)* `/amendment/search` and `/amendment/section/search` return **amendment, commencement, repeal and revocation** relations: provision-to-provision, a resolvable URL on both sides, a `type_of_effect` vocabulary, and a **`search_amended` flag that carries the direction** (`True` = made *to* this instrument, `False` = made *by* it). Add one or two Worker tools over them and teach Phase 2 to use them for any relationship question. **This is what 6340, 6409 and 6410 needed** — they were answered from search-result adjacency because nothing could answer them properly. **Design notes, all from the probe, so do not re-derive:** (a) rows are a **bare JSON list**, not `{results: []}` — the same shape trap `_slim_section_results` already handles for section search; (b) **19% of rows carry `type_of_effect: null`** (261 of 1,358 sampled) and a tool that renders those as a relationship will assert nonsense — decide explicitly whether to drop or label them; (c) **there is no date on a commencement row**, so "s.31 was commenced by SSI 2020/295" is retrievable but "on 8 October 2020" needs a second hop into that SSI's text or `description` — which bears directly on **P2.5**, since an in-force claim needs the date; (d) `size` is the only visible control and completeness is unconfirmed — one of the residual LEX questions. **Acceptance** *(deterministic + replay)*: a unit test over the slimmer (bare list, null effect type, both directions), then **6340's answer must either cite a retrieved relation or say it could not be verified** — the same bar P2.3 sets, now passable. **Evidence:** 6340 6409 6410 6354 6383. **Depends on:** P2.3 **VERIFIED LIVE 2026-09-16 (end of Session 7), so the row can be built without a discovery pass — and one of its own implications is corrected.** **(1) The data is there, and it refutes the pre-pilot answer directly.** 6409 asked *"what sections of the Social Security (Amendment) (Scotland) Act 2025 have been commenced by regulation?"* and was told **"No commencement regulations have been made yet"**. `/amendment/search` on `asp/2025/2` with `search_amended=True` returns **71 rows, every one `coming into force`, of which 15 are commencements BY an SSI** — ss. 9, 20 and 21 (among others) commenced by **`ssi/2025/119` reg. 2 sch.**, which is the instrument Session 6 verified as **held** in LEX. That is the acceptance evidence, already found. For 6382/6383's `asp/2018/9`: **956 rows, 368 `coming into force`, all of them by SSI**, including `ssi/2020/295` and `ssi/2018/298` — the instruments 6383 speculated about. `ukpga/1998/46` (6374) returns 956+ rows too. **(2) `size` SILENTLY TRUNCATES, and there is no total — do not repeat P1.3's defect.** `asp/2018/9` returns exactly **200 rows at `size=200`, 500 at `size=500`, and 956 at `size=2000`**. The response is a bare list with **no count field of any kind**, so a tool that passes a modest `size` will under-report relationships with nothing to signal it — the same windowing defect `search_legislation` had, where `total` was misreported on 1,010 of 1,531 searches. **Either fetch past the cap or state the window, exactly as `legislation_search_note` does.** **(3) The row shape, confirmed** — `changed_*` / `affecting_*` pairs (`legislation`, `year`, `number`, `url`, `provision`, `provision_url`), plus `type_of_effect`, an `id`, and three always-null `ai_explanation*` fields. **No date field**, as recorded. Commencement rows are frequently **self-referential** (`asp/2025/2` s.27(1) commencing its own s.28) — 56 of 6409's 71 rows are the Act commencing itself, NOT a regulation, so a tool that does not separate the two will answer "commenced by regulation" wrongly. **(4) CORRECTION to this row's own acceptance: P3.5 does NOT fix 6340.** `ukpga/1962/47` returns **0 rows in both directions** — it is a 404 in LEX, so there is no relation to retrieve. 6340's bar is met only by the *negative* branch ("could not be verified"), which **P2.3 already delivers and its acceptance measured**. Pick the acceptance session accordingly: **6409 and 6410 are the ones this row can turn from a false negative into a retrieved fact**, and 6383 for amendment relations. **BUILT AND ACCEPTED (Session 8, 2026-09-16). The row's premise held and three of the facts handed to it did not survive contact with the feed — two of them change what the tool reports.** **(1) 35% of rows are `http`/`https` duplicates of ONE relation, and the API's own `id` embeds the scheme, so deduping on `id` removes nothing.** Measured over 6,738 rows on eight instruments in both directions: 4,363 distinct, **2,375 duplicates (35%)**, concentrated in exactly the Scottish material this corpus is about — `asp/2025/2` returns **71 rows for 36 relations**, `asp/2018/9` **956 for 484**, `asp/2014/18` **607 for 309**, while `ukpga/1998/46`, `asp/2000/1` and `ukpga/1981/67` have none at all. ~~71 rows, 15 of them commencements by `ssi/2025/119`~~ — **that is the double count: 36 relations, 8 by an SSI, seven by `ssi/2025/119` and one by `ssi/2025/377`**, the instrument the text index 404s on and whose number 6409 was asked to re-check. The dedupe key is `(changed_legislation, changed_provision, affecting_legislation, affecting_provision, type_of_effect)`, scheme-free by construction. **(2) The cap CAN be fetched past, cheaply, so the tool does that rather than stating a window.** Measured over the distinct `legislation_id`s the replay corpus actually touched (**272** at the last run): median **12** relation rows, p90 **870**, **13 (4.8%) over 2,000** — every one of which completes at 20,000 (largest 5,185 rows / 5.3 MB / 2.3 s). `ukpga/1988/1`, which the corpus never touches, reaches **13,681 rows / 11.9 MB**. So the fetch starts at 2,000, escalates once when exactly that many come back (which can only mean the cap bound), and states the window only if the second attempt also binds — never observed. **(3) `type_of_effect: null` is LABELLED, not dropped**, decided explicitly as the row asked: the row still records that an instrument changed a provision, so dropping it would make the tool the reason a relation went missing. It renders as the literal `"not stated"` and the block forbids calling such a row a commencement, an amendment or a repeal. **(4) Provision LABELS, not provision URLs** — a URL beside every listed provision costs 45-70 KB on the large Acts (`ukpga/2004/33` 26 KB -> 70 KB) against 3.7 KB on `asp/2025/2`, for a citation form a commencement answer does not use. The subject Act's URL and each related instrument's are emitted, which is what the answer cites; measured cost of the choice is **1 of 51 turns** carrying a P1.6 dagger. **Built at the four seams P2.2 and P2.3 established** (`src/utils/search_scope.py`), plus the prompt and the tool set: (a) `amendment_search_note` on every result, stating the self-referential split, the unstated-effect rows, the window, and — on an empty record — that nothing being *recorded* is not nothing having *happened*; (b) `record_relations` + `_relations_limb` on `worker_scope_block`, because the Manager and the DR synthesis never see a tool result and they are the agents that write "no commencement regulations have been made"; (c) one clause on `answer_scope_footer`, gated on the **structural** fact that this turn consulted a change record; (d) the strip widened to `CHANGE RECORD` — the same live defect P2.3 hit with `[ENABLING POWER …]`, caught here by writing the test first; (e) `_RELATIONSHIP_RULE` on all three legislation worker prompts, deliberately adjacent to `_ENABLING_POWER_RULE` so the two read as a pair; (f) a one-sentence routing clause on **both** search blocks, which is the seam where the false negative actually forms. **Three wiring decisions, each recorded where it is made:** in `CACHEABLE_TOOLS` (a `legislation_id` and a direction in, published statutory data out — no user content on either side); **UNCLASSIFIED for phase**, like `get_member_info`, because counting it Phase 2 would raise `phase2_retrieval_calls` without raising `sources_kept` and move every efficiency number published in Waves 0-2; keyed for redundancy on `legislation_id` + `direction`, since the two directions are different questions and `max_redundant_tool_calls` is 0 on this profile. **ACCEPTANCE PASSED** (`evidence/replay/wave3_p35/`, n=3 on 6409, 6410 and 6383 — 9 runs, **$4.10**, 37 min, zero model mismatches). Graded per TURN with `replay_report --dir <dir> commencements`, whose headline is a fact check against a ground truth `lex_probe --commencement` re-prints live. **Turns delivering the relation went 0 of 8 to 24 of 24; flat false negatives 6 of 8 to 0.** The before-column is zero in every wave — `baseline/` 0 of 8, `wave1/` 0 of 8, `wave2_p22_final/` 0 of 18 — because P2.2 changed the *shape* of the failure and not the outcome: *"No commencement regulations have been made yet"* became *"A search of the legislation index … did not return any results"*, honest and still not the answer. **Invariant 1 held in both directions.** Answers grew in **13 of 17** matched turn slots (6410 t2 87 -> 900 chars, 6409 t5 147 -> 944), no scope block leaked in any of 51 answers, and the four that shrank are all the model no longer padding a non-answer — 6409 t10 went from *"Are you certain of the SSI number and year?"* (**P2.4's exact failure**) to confirming the citation against the change record. **The discovery loop collapsed**: tool calls per answered turn **10.4 -> 4.4** across the same three sessions, with `search_legislation` 130 -> 101 over three times as many runs. **P2.3 did not regress**: 1 of 51 turns asserts an unverified derivation, against 2 of 29 in its own acceptance. **ONE TRAP THIS ROW LEFT OPEN, found at P2.5 (Session 9) and closed there:** `type_of_effect` has a **second** commencement string, `Commencement Order` (1,355 relations over the corpus's legislation_ids), and it is NOT the subject's commencement — its `changed_provision` is one of eight placeholder values and never a provision of the subject, because the row records another Act's commencement order bringing into force an **amendment to** the subject. `ukpga/1998/46` has 29 of them and zero `coming into force` rows, so a model asked what commenced the Scotland Act 1998 was handed 29 rows and a block inviting it to state any of them. Session 8's probe read "zero commencement rows" for that Act because it filtered on the exact string, which is right for the question P3.5 asked and hid this one. Also corrected: ~~there is no DATE on any relation~~ is true where it matters and not literally — 552 of 89,465 relations embed one in the effect string (`saved (6.5.1999)`), but **0 of the 19,031 `coming into force` rows do**, so the second hop is still required and the block's wording stands. **One detector correction, found by the acceptance run and re-validated in both directions**: a denial of the **remainder** (*"no further commencement regulations have been found"*, said by a turn that named `ssi/2025/388`) is the right answer, not the defect — and the exclusion is scoped to the noun phrase, because `wave2_p22`'s *"no commencement regulations bringing further sections into force were identified"* attaches the qualifier to *sections* and is a flat denial. Re-run over all six historical directories: **not one before-column number moved.** **An unlooked-for result: P2.5 becomes answerable by a second hop.** The relation carries no date, and 6409 rep 2 did what the block tells it to — retrieved `ssi/2025/119` and read *"into force on 10 May 2025"* out of `legislation.description`. **833 tests** (764 -> 833). Full numbers: `BASELINE.md`, *B3*. |
| `[ ]` | **P3.6** | **Stop discarding `description`, which is where the relationships are.** *(New row from P5.1, Session 5.)* `_slim_search_results` strips `description` deliberately — the comment calls it "verbose and redundant once Phase 2 retrieves actual section text". **True of the text, false of the relationships**: no section text says what an instrument commences, but the description does, *with the date* — *"These Regulations bring sections 31 and 36 and schedules 5 and 10 of the Social Security (Scotland) Act 2018 into force on 8 October 2020."* **5 of 10 sampled results** carried commencement, amendment or enabling-power language there. It is also the only observed route to an **enabling power**, which P3.5 cannot supply. **The original reason for stripping it was real and must not simply be reverted** — it was ~10–16K per result and removing it is what kept Phase 1 under the summarisation threshold (see `CLAUDE.md`). So: keep it truncated (the sampled descriptions are 73–210 chars), or keep it only where it matches relationship language, and **re-measure the Phase-1 summarisation rate afterwards** — P1.4 already showed payload size drives it. **Acceptance** *(deterministic)*: a unit test that `description` survives slimming within a char cap, plus `phase1` summarisation calls unchanged over a replay directory. **Evidence:** 6340 6409 6410. **Depends on:** — **CORRECTED at P2.3 (Session 7): `description` is NOT discarded everywhere, and the enabling-power argument for this row is weaker than it reads.** `_slim_search_results` strips it from *search* rows, but `get_legislation_text` returns the `/legislation/text` response unslimmed and `legislation.description` is exactly where the recital lives — so the enabling power is already reachable and P2.3 did not need this row. What P3.6 buys is the same field at **Phase 1**, without a retrieval: worth having for commencement dates (P2.5) and for cheapness, not for reachability. And measure before promising: `lex_probe --enabling` finds a recital in **18 of 25 pre-1990 UK SIs and 0 of 28 SSIs of any era**, so on the Scottish corpus this row adds no enabling-power coverage at all. **Measured at P2.3 (Session 7), and it confirms the strip is total:** across **5,299** `search_legislation` result rows in the post-Wave-1 replay corpus, **0 carry a `description`**. So there is no partial leakage to rely on and no existing behaviour to preserve — whatever this row adds is the only `description` Phase 1 will ever see. |
| `[ ]` | **P3.7** | **`/legislation/lookup` — a deterministic held/absent test, which keyword search cannot be.** *(New row from P2.2, Session 6, per the re-planning protocol — a retrieval fix, where P2.2 is the disclosure that has to stand until it lands.)* Four of the corpus's negatives are the same question: *is this instrument, named by number, in the index?* — 6409 turns 9-11 (`SSI 2025/377`, asked three times) and 6373 turn 2 (`SSI 2026/170`). All four were answered by `search_legislation`, a **ranked keyword search**, which cannot answer it: absence from the top 5 of a median 141 matches is not absence from the corpus, so the model reported a miss and then **asked the lawyer to check their own citation**. Both citations were right. **Both instruments are a 404 in LEX** (verified 2026-09-15), so the honest answer existed and no tool could reach it. `POST /legislation/lookup {legislation_type, year, number}` returns **200 or 404** — one of the ten endpoints P5.1 found we never call, and the only one that turns "I could not find it" into "it is not held". Add it as a Worker tool and route any question naming an instrument by number to it first. **Design notes:** (a) a 200 does not imply text — `ssi/2025/119` is held with an empty body (P5.3), so the tool must report held-vs-text separately or it swaps one false negative for another; (b) `/legislation/section/lookup` is the provision-level equivalent and 404s where the instrument is a stub, which is the signal that distinguishes them; (c) this is the ONLY row that can make a negative *definite* — P2.2 can only make it honest, which is why P2.2 does not wait for it. **Acceptance** *(deterministic + replay)*: a unit test over held / 404 / stub, then 6409 turn 9 says `SSI 2025/377` **is not held in the index** rather than asking the lawyer to verify the number. **Evidence:** 6409 6373 6410. **Depends on:** — |

### Wave 4 — Interaction (independently shippable, mostly frontend)

| | ID | Row |
|---|---|---|
| `[ ]` | **P4.1** | **B7 — The research-mode dead-end.** Carries the single worst user experience in the corpus: 6346 is the only 1/1 session, where MoniqueM changed the mode, said so, and was told four times she had not. She opened a fresh session, asked the identical question, and got a full answer first try — that recovery is the confirming case. **Code-checked and unchanged: the backend is not stale.** `researchMode` is sent on every request and the mode note is rebuilt per turn; the model anchors on its own earlier refusal, still in the conversation history, over the current system prompt. Three separable bugs: **(a)** the anchoring — inject a mode-change marker into the history when the resolved mode differs from the previous assistant turn's (needs the previous turn's mode, which the frontend has and discards — see D13); **(b)** the wrong control name — `prompts.py:649-650` says "'Legislation & Case Law' mode via the mode selector"; the real control is **Research filters → "Legislation & case law"**, and the transcripts show the model paraphrasing it to "Research mode", which is the *chat mode* control and does not lift the case-law block; **(c)** invented UI — in 6343 it told the user the control is "typically located at the top or side of the chat window". Strip every speculative UI description. Also: AlistairC clicked AILA's own suggestion chip in 6407 and was told "I cannot change the mode for you" — make the chip perform the switch or stop offering it. **Acceptance** *(replay, n=3)*: scripted two-turn sequence — refusal, mode change, same question; assert the second answer does not reference the old mode. **Evidence:** 6346 6343 6347 6350 6407. **Depends on:** P2.* |
| `[x]` | **P4.2** | **B13 — Lost turns, blank replies, latency.** **15 user turns across 11 sessions never received a reply**; two assistant messages rendered blank — 6370 #8 **billed $0.0217** with nothing shown, 6363 #6 billed nothing. 6387 timed out on a follow-up. ~~n=2 on the blanks, so reproduce before fixing.~~ ~~**Reproduced in the P0.3 baseline: 4 billed-but-empty turns (6370 ×2, 6407 ×2), all `status: ok` with `audit.error: null`.**~~ **An undercount, corrected at the build (Session 10): there are NINE blank-body turns across the ten replay directories, EIGHT of them billed — 6370 ×2, 6406, 6407 ×2, 6338, 6359, 6383, plus 6374 r3 t3 at $0. 8 billed blanks in 616 turns (1.3%), six sessions, three chat modes. `python -m tools.replay_report --dir <dir> blanks`.** **Suspect (1) is eliminated by the evidence:** none of the four emitted a single `token` event, so there was never a body for the `<suggestions>` strip to consume. Start at suspect (2). Suspect (3) is unsupported — no error event on any of the four. **The acceptance test cannot name a turn:** this row cites 6370 #8, but the replay blanked at turns 2 and 3 while the pre-pilot's turn 2 answered normally — assert the invariant (non-empty body whenever recorded cost > 0), not a session-turn pair. The *timeout* half did **not** reproduce: 6387 answered all three turns. The billed-but-empty case is the informative one: content was generated and lost downstream. Eliminate in order: (1) a `<suggestions>` strip consuming the whole body — `utils/suggestions.py` takes the **last** block and also strips an *unterminated* trailing tag, which is exactly what a partial stream looks like; (2) a tool-call-only final message; (3) the `chat_loop` stream-retry guard re-raising after partial emission. **Suspect (4), added 2026-09-15 and now the leading one: the provider returns an empty final message and we cannot see that it did.** `chat_loop` (`agent/openrouter_client.py`) accumulates `delta.content` and `delta.tool_calls` and **never reads `finish_reason`**, so an empty stream becomes `{"role": "assistant", "content": ""}` and, with no tool calls, is returned as the answer with `status: ok`, no error and full billing. That is the B13 signature exactly, and it explains "no `token` event" without requiring anything to be lost downstream. Three public reports describe this shape on this model family: Google has **reproduced and is fixing** Gemini 3 streaming + function calling ending `finish_reason=STOP` with empty text after a tool executes -- absent in unary mode and on 2.5-flash (<https://discuss.ai.google.dev/t/gemini-3-5-flash-with-streamgeneratecontent-automatic-function-calling-stops-after-tool-execution-and-returns-empty-final-text/170528>); Gemini's `MALFORMED_FUNCTION_CALL` is silently normalised to `finish_reason='stop'` with empty content and zero tool calls, *indistinguishable from the model choosing to say nothing* (<https://github.com/BerriAI/litellm/issues/21744>); and OpenRouter emits duplicate `finish_reason` chunks on tool calls, the second one empty (<https://github.com/QwenLM/qwen-code/issues/2402>). OpenRouter's own docs add that models occasionally return no content during warm-up/scaling events, and that mid-stream failures arrive as `finish_reason: 'error'` carrying `native_finish_reason` (<https://openrouter.ai/docs/api_reference/errors-and-debugging>). **This is a lead, not a diagnosis** -- those reports are the google-genai SDK with *automatic* function calling and cite 3.5-flash / 3.1-flash-lite / 3.1-pro, not our OpenRouter manual tool loop on `google/gemini-3.1-pro-preview`. **So the first move is a diagnostic, not a fix:** capture `finish_reason` and `native_finish_reason` in `chat_loop` and surface them on the audit trace (additive, fail-soft, a few lines). Until that lands all four suspects look identical in the data, which is why this row has never been able to name a cause. It also means **the fix may not be ours** -- if it is the upstream streaming bug, the answer is a bounded retry on an empty final message, not a parser change. **New evidence from the Wave 1 sweep: 6338 turn 2** -- 2 delegations, real API calls, 330 s, $0.3996, `status: ok`, no `token` event, empty body. B13 is **not confined to 6370/6407**, which is what this row was scoped against. Separately, for perceived latency (MoniqueM reported a 5.5-minute turn as "around 15 minutes"), add a coarse step counter — the `tool_call` / `api_call_start` events already exist behind `emit_tool_details` and cost nothing in retrieval terms. **Acceptance** *(deterministic)*: assert a non-empty body whenever recorded cost > 0. **Evidence:** 6387 6370 6363 6345 6335 **6338** (Wave 1 sweep). **Depends on:** — (independent of the accuracy path) | **A NEW AND WORSE INSTANCE OF THIS BUCKET, found at P2.5 (Session 9), and the code has no guard against it.** 6383 rep 1 turn 4 of P2.5's acceptance sweep returned a Deep Research report whose body was **empty** - the lawyer saw the scope footer and nothing else - with `status: ok`, no error, 30 tool calls, three intact step reports (4,836 / 5,777 / 7,190 chars) and 219 commencement relations retrieved. **First blank in 218 answered turns across eight replay directories**, and 6383's DR turn produced 9,190 / 6,503 / 7,490 / 8,672 chars on the four prior runs. **The cause is an empty provider completion at the synthesis call**, not the scope strip: the request went out at `tools=0, msgs=2, ~21421 chars` and the task finished **9 seconds later**, and `strip_scope_blocks` never fired (it logs when it does; the log has no such line). **Two gaps this exposes, both additive to fix.** (1) `chat_loop`'s stream retry cannot catch it: it retries only while nothing has been emitted, and a successful 200 carrying no content chunks raises no exception, so an empty completion is indistinguishable from a finished answer. (2) `run_deep_research` never checks that the synthesis produced anything - a blank report is composed, footered and returned. The obvious fix is a fallback to the step findings when the synthesis comes back empty, which is strictly better than nothing and needs no new prompt. **n=1 and the re-run is the test**; P2.5's prompt change to `DEEP_RESEARCH_SYNTHESIS_PROMPT` is not excluded as a contributor, but no mechanism links a longer instruction to an empty completion and an empty 200 is known provider behaviour. **Evidence:** 6383. | **DONE (Session 10). The cause is an empty provider completion, and the code could not see one.** The diagnostic landed first as the row required: `finish_reason`, `native_finish_reason`, `reasoning_chars`, `completion_tokens` and a mid-stream `error` payload are now captured in both clients and surfaced as **audit schema v3's `empty_completions[]`** (`[]` on a healthy request; `AUDIT_TRACE.md` updated). They separate the four mechanisms the stored evidence could not tell apart — and the `error` payload is the one that was **ours**: OpenRouter reports a mid-stream failure that way and `chat_loop` read only `usage` and `choices`, so such a stream ended as an ordinary empty completion with `status: ok`. **The question the row posed about 6383 is answered and it changed the fix:** 6383's synthesis call declared NO tools (`tools=0, msgs=2`), so the failure is **not** confined to tool-call turns and a guard scoped to them would have missed it — the retry keys on an empty completion, full stop. It is stochastic, not deterministic (same payload succeeded five times, failed once), which is the condition a bounded retry answers. **Built:** (a) `chat_loop` in BOTH clients retries a clean 200 carrying no content and no tool calls, reusing `_MAX_STREAM_ATTEMPTS`/backoff; a tool-call-only message is never retried (that is the normal ReAct shape and replaying it re-runs the turn's research), and the discarded attempt's cost is banked so the turn's recorded spend stays honest. (b) `run_deep_research` falls back to the step findings, (c) the Manager falls back to the worker reports it already holds, or to a plain notice when there are none — **both labelled as fallbacks under Invariant 1**, since research the answering step never used is not an answer and must not read like one. **The latency half is built too:** a step count and an elapsed clock in the status line, both derived client-side from data already arriving (`tool_start` events, the run's existing `startedAt`) — no new SSE events, no backend change. **No denominator, deliberately**: outside Deep Research nothing knows the total, so "step 3 of 8" would be a claim about how much is left. **Acceptance PASSED:** `replay_report blanks` asserts the invariant over stored run files (exit 1 on a violation) and reproduces the hand count exactly on all ten directories. It grades the **body**, not the answer — since P2.2 a blank turn is 1,293 characters of scope footer with nothing above it, and grading `answer` scores that turn as fine. Validated both directions: the shortest bodies it leaves alone are real 46–50 char answers. **998 tests** (963 → 998). Live-verified end to end: a real research turn returned a full report, and the status line read *"Researching · 13 steps · 1m 01s"*. **B13 closes.**
| `[ ]` | **P4.3** | **B8 — The sources rail and the answer disagree.** Both directions: 6378's correct instrument (SSI 2008/216) was **in the rail and absent from the answer** ("This legislation was cited in sources but not in the answer"), while 6372 quoted provisions as belonging to one instrument when they belong to another. 6335 and 6359 carry sources the lawyers checked and found irrelevant, which costs checking time and erodes the rail as a verification surface. Invert the existing `_source_is_used` check into a diagnostic — a source retrieved, kept and not referenced is a detectable condition — and surface it both to the model as a pre-answer check and to the rail as a cited/consulted distinction. **Acceptance** *(replay, n=3)*: 6378 contains SSI 2008/216 in the answer body; instrument the unused-source rate across the whole replay set (one observation was never a rate; there are now four). **Measure it on report turns only — corrected 2026-09-15 during P1.5.** The baseline's headline 88% (1,222 of 1,387) adds two conditions together: on turns that produced a report it is **489 of 622 (79%), 9 turns citing none**; the other **765 sources and 53 turns hang off turns that never answered** — B1 halts and B3 false negatives with the rail still showing what the Worker accumulated (6341 turn 5: **40 sources behind a 333-char "timed out" message**). Those are not the model declining to cite a source. **Consequence: this row now depends on P2.1** — fixing the halt removes a large share of the 88% without touching the rail, so a number taken before P2.1 will move for reasons unrelated to this row. **Evidence:** 6378 6372 6335 6359 6367. **Depends on:** P3.1, **P2.1** (added — see above) |
| `[x]` | **P4.4** | **Remove the case-law court filter.** *(New row, Session 5, from P5.2's probe — and DONE in the same session.)* **Three reasons, and the third made it a defect rather than dead weight.** **(1) Nobody used it** — zero of the 41 replayed pre-pilot sessions set a court, against `current_only` at 29 and `jurisdiction` at 13; only 11 of 62 sessions touched case law at all. **(2) For a Scottish Government audience it was a trap** — all 14 options are English, Welsh or UK-wide, because the National Archives corpus holds no Scottish courts at all (`court=csoh` → **400, "not one of the available choices"**, P5.2), so selecting any of them guaranteed a non-Scottish result set and narrowed a corpus that was already the wrong jurisdiction. **(3) It OVERRODE the model** — `executor.py` applied `_court` *after* the model's own `court` argument and clobbered it, so a court chosen three turns earlier silently beat the model's per-query judgement: the same stale-filter failure as B2 and B4. The neighbouring date filters *intersect* (`max`/`min`); court did not. **The capability is NOT removed, only the control** — `search_case_law` keeps its `court` parameter, so AILA still narrows by court when a question calls for it, which is the behaviour the filter was standing in front of. **Retired on P1.2's precedent exactly:** request field removed outright (pydantic ignores rather than rejects, so a stale client still gets a 200 — verified live), `audit["filters"]["court"]` kept as a permanent **null** so the trace shape does not move under the eval harness, and `AUDIT_SCHEMA_VERSION` **not** bumped. Removed: the UI control and its `COURT_GROUPS` vocabulary, `caseLawCourt` state, its localStorage keys and per-chat snapshot field, the filter chip, the request field on all three routers' shared model, `_court`, the executor override, the prompt's constraint line and `_COURT_LABELS`. **Kept deliberately:** the tool schema's court codes; `redact.py`'s `court` (a model tool arg, still sent); the **export column** and historical `filters` rows, because pre-pilot snapshots recorded real values and an export that dropped them would misrepresent what those sessions ran under; and `court` in the replay harness, which records historical configuration. **The jurisdiction panel's Scotland/NI note stays** — it is the true statement the court list was quietly contradicting. **Acceptance passed** *(deterministic)*: `tests/test_court_filter_removed.py` (9 tests — field gone, stale client ignored not rejected, neighbours intact, no prompt line, no stale-key resurrection, override gone, **tool parameter and model-supplied pass-through both still present**, audit null), 643 tests green, `client/dist` rebuilt. **Note what this does NOT fix:** removing a misleading control does not put the Court of Session in the corpus. **P2.4 is still required** and P5.2's question still stands. **Depends on:** — |

### Wave 5 — External (open now, in parallel; unknown lead time)

**Drafted and ready to send, 2026-09-15 (Session 5): `WAVE5_QUESTIONS.md` next to this file.**
All three questions are written out in full with the evidence attached; they need a human to send
them, which is the only reason they are still open. Each row's acceptance is a recorded answer, so
when a reply arrives paste it into `SESSION_LOG.md` and tick the row — and for P5.1 record the
*consequence* too, since "no relationships" makes **P2.3 permanent** rather than interim.

| | ID | Row |
|---|---|---|
| `[x]` | **P5.1** | **B3(a) — Ask the LEX team whether relationships are retrievable.** Does the API expose enabling-power, amendment, commencement or revocation relations in any form? This determines whether the largest bucket is **buildable or permanently a disclosure**, and P2.3 is the holding fix either way. Known from direct probing: `/legislation/search`, `/legislation/section/search` and `/legislation/text` are the only endpoints we call; `text` returns a record for `ssi/2025/119` with an **empty `text` field** while `section/search` returns nothing for it, so stub records exist and are indistinguishable from absence at the tool boundary — worth raising in the same conversation. **Acceptance** *(external)*: a recorded answer in `SESSION_LOG.md`, and either a new row or a note that P2.3 is permanent. **Depends on:** — | **ANSWERED 2026-09-15 (Session 5), and largely without them.** The API publishes an OpenAPI spec at **`/openapi.json`** listing **13 endpoints; we call 3**. Nobody had looked. **Amendment, commencement, repeal and revocation are all retrievable** via `/amendment/search` and `/amendment/section/search`: provision-to-provision, a resolvable URL on both sides, a `type_of_effect` vocabulary (246 "coming into force", 43 "Commencement Order", 62 "repealed", 6 "revoked" over 1,358 sampled rows), and a **`search_amended` flag that carries the direction**. **Only *made under* has no route** — not in `/legislation/lookup`'s fields, and explanatory notes are Act-level (404 for `ssi/2020/295`). **So B3 is BUILDABLE, not permanently a disclosure**, for four of its five relations → **P3.5**; P2.3 narrows to enabling power alone. Separately: `description` on every search result states relationships in prose *with dates* and `_slim_search_results` discards it → **P3.6**. **The stub question in this row was wrong and is corrected:** `/legislation/text` returns `{legislation, full_text}`, the nested `text` key is empty for *everything* including fully-held Acts, and a genuine stub is explicitly signalled (`full_text` = "No text content available for this legislation.", plus `/legislation/section/lookup` 404) — stub, held and absent are three distinguishable states. Evidence is reproducible: `python -m tools.lex_probe`. **Five narrower questions remain for the LEX team** (`WAVE5_QUESTIONS.md`), led by enabling power and the missing commencement date. **Acceptance met** — answer recorded in `SESSION_LOG.md` and here.
| `[~]` | **P5.2** | **B12 — The Scottish case-law corpus.** The Court of Session and Sheriff Courts are not in the National Archives corpus. In 6375 that stopped being a coverage gap and became a wrong answer. Also flagged by AlistairC (*Clark* absent, 6359), CambeulW (recency bias, 6363) and EmmaM. This is a product/procurement question, not an engineering one; P2.4 is the engineering half and is not blocked by it. **Record it as the top substantive content gap of the pre-pilot.** **Acceptance** *(external)*: a recorded decision. **Depends on:** — | **A lead found 2026-09-15 (Session 5) that may change this from procurement to an API request.** LEX's own `/healthcheck` reports case-law collections in its vector store — **`caselaw` 69,970 points, `caselaw_section` 4,723,735, `caselaw_summary` 61,107** — while `/openapi.json` exposes **no case-law endpoint at all**. A case-law corpus therefore exists inside a system we already call, and we reach case law through the National Archives instead. **Ask whether it is reachable and whether it includes the Court of Session and the Sheriff Courts before treating this as a purchase.** Reproduce: `python -m tools.lex_probe --coverage`. **FACTS ESTABLISHED 2026-09-15 (Session 5); the decision itself is not mine and the row stays open.** **The gap is verified, and it is narrower than "no Scots law":** TNA offers **42 court codes, none Scottish and none Northern Irish** (scraped from the search facet), and `?court=csoh`/`csih` are rejected **400 — "csoh is not one of the available choices"**. **But Scottish appeals that reached the UK Supreme Court ARE indexed** — *Daly v HM Advocate (Scotland)*, *ABC v Principal Reporter (Scotland)*, *X v Lord Advocate*. So what is missing is **the Court of Session, Sheriff Appeal Court, Sheriff Courts and High Court of Justiciary** — where the overwhelming majority of Scots law is actually made — not Scots law entirely. State it that way: the blunt version is false and would make a lawyer distrust a sound UKSC result. **And the failure mode is a wrong answer, not an empty one:** a `query=Court of Session` search returns 200 with 50 results of which **not one is a Scottish judgment** (20 EWHC, 11 UKFTT, 5 EWFC, 4 UKSC, 3 UKUT, 3 EWCA, 2 EWCOP, 1 EAT) — English authority that merely mentions Scotland, which the model then answers from. That is 6375 precisely, and it **raises P2.4's priority**: an empty result is a disclosure, a full one of the wrong jurisdiction is a trap. **Every court AILA can filter on is non-Scottish** — 14 options in the UI and the tool schema, none Scottish (the codes are correct; this is the product gap, not a filter defect). ~~**Ask the LEX team before buying anything:** … if that corpus covers the Court of Session this is an API request to a team we already work with.~~ **Largely closed off by user knowledge, 2026-09-15 — and the row's real subject is now FINDING AN ALTERNATIVE CASE-LAW API.** The probe found `/healthcheck` reporting `caselaw` **69,970**, `caselaw_section` **4,723,735** and `caselaw_summary` **61,107** points while `/openapi.json` exposes no case-law endpoint and the conventional mirror names all **404**. **The user recalls that LEX *used to* expose case law and has since DISABLED access, and that Scottish courts were not represented in that data anyway.** *(Recollection, recorded as such — not verified. It is consistent with what the probe found, which cannot distinguish "disabled" from "never exposed".)* Two consequences: the LEX question drops from "is this reachable" to a **confirmation** worth one line in the email, and even a yes would probably not deliver the Court of Session. **So the substantive work is sourcing case law elsewhere**, which is a different question from the licence one this row was written around. **Candidates, none assessed:** **BAILII** carries `ScotCS` (Court of Session), `ScotHC` (High Court of Justiciary) and `ScotSC` (Sheriff Court) and is the obvious technical fit — **but check its terms of use before designing anything, as they restrict automated access, and discovering that late would waste the build**; the **Scottish Courts and Tribunals Service** (`scotcourts.gov.uk`) publishes opinions directly and should be checked for a feed or API; and the commercial providers (Westlaw, LexisNexis) which are a procurement question with a licence cost. Whatever is chosen must also clear the **internet-restricted target's whitelist** — see `CLAUDE.md` → External API Dependencies — which rules out anything needing broad outbound access. Brief and the decision as three steps: `WAVE5_QUESTIONS.md`. **P2.4 is not blocked by any of it** — whatever the corpus holds, an answer drawing on English authority for a Scots-law question must say so, in code, every time.
| `[x]` | **P5.3** | **Corpus currency.** Verified absent from LEX in September 2026: Education (Scotland) Act 1962 (`ukpga/1962/47` → 404), SSI 2026/170 → 404, SSI 2025/119 (record exists, text empty), stub-only provisions for the Education (Scotland) Act 1945. Ask what the index's coverage rules and refresh cadence actually are, so B5's "absent from the index" wording can be accurate rather than generic. **Acceptance** *(external)*: a recorded answer. **Depends on:** — | **ANSWERED 2026-09-15 (Session 5), the same way as P5.1 — `GET /api/stats`, `GET /healthcheck` and direct coverage sampling; `python -m tools.lex_probe --coverage`.** **The index is refreshed DAILY and is current** — ingestion runs land ~02:00–02:30 UTC and the newest `created_at` observed was **today, 02:17**. Corpus: **220,022** instruments, 2,107,361 provisions, 2,580,915 amendments. **So staleness is not the problem; per-instrument gaps are, and for 2026 they are severe:** ASP 2025/26 **100%**, SSI 2025 **85%**, ~~**SSI 2026 5%**, **UK SI 2026 0%** (20-point samples)~~, UKPGA 1962 **60%**. **Two of those five were corrected at P2.2 (Session 6) by the cross-check this row itself prescribes, and one of them in the direction that matters.** On a 60-point census: SSI 2026 **2%**, UK SI 2026 **2%**, SSI 2025 **87%** — and "UK SI 2026 0%" is the number to retire, because it *reads* as "nothing from 2026 is held", which is false: `/legislation/search` returns **27 distinct `uksi/2026/*` instruments**, and all 12 spot-checked resolve on `/legislation/lookup`. Low single digits, not nothing. The product wording that came out of this row therefore says "under 5%" and never "none" — a lawyer told "none" stops looking. All three original claims re-verified: `ukpga/1962/47` absent — **and it is a per-instrument gap, not a date cliff**, since `/41 /42 /45 /50 /51 /55` are all held, so "we don't hold pre-19xx" is the wrong story to tell a lawyer; `ssi/2026/170` absent, consistent with ~5–20% coverage of that series; `ssi/2025/119` an explicit stub. **This directly sets B5's wording (P2.2): on these numbers a "not found" for a 2026 SSI is far more likely a coverage gap than an absence in law**, and a negative that does not say so is close to telling a lawyer the instrument does not exist. **Method warning, and it cost two wrong conclusions in this probe:** point lookups are a bad census instrument — twelve misses across `ssi/2026/{1..250}` read as "no 2026 SSIs at all", and `uksi/2026/772` was in the index the whole time, created a week earlier. Cross-check a lookup census against `/legislation/search`. **One finding belongs to P5.2, not here:** `/healthcheck` reports **case-law collections** (`caselaw` 69,970, `caselaw_section` 4,723,735, `caselaw_summary` 61,107) that **no endpoint in `/openapi.json` exposes**. **Remaining for the LEX team** (`WAVE5_QUESTIONS.md`): why is 2026 only ~20% ingested when the refresh is daily; is the older patchiness inherited from legislation.gov.uk; is there a published coverage statement. **Also now on the list, for the National Archives rather than LEX: what is the case-law corpus's DATE coverage?** Judgments exist back to **1965**, but neither the Atom feed (capped at 50 per query, every window) nor the search page exposes a total, so density before ~2000 could not be measured here — see the Session 5 log, where an unverified "coverage starts ~2001" claim was made and retracted. CambeulW's recency-bias complaint (6363) may well be sound; it is currently **unmeasured**, and it would affect English research too, not only Scots.

---

## Bucket → row index

Use this to get from a bucket in the analysis to the rows that close it.

| Bucket | Rows |
|---|---|
| B1 Deep Research halt | P2.1 |
| B2 Jurisdiction filter | P1.1 |
| B3 Relationship blindness | P2.3 (holding, enabling power only) **done**, P3.5 (retrieval) **done**, P5.1 (answered) |
| B4 In-force status | P1.2 (filter), P2.5 (model) |
| B5 False negatives / silent substitution | P1.3 (data), P2.2 (disclosure), P2.8 (carried-forward negatives), P2.9 (memo gap in the disclosure), P3.7 (held/absent test) |
| B6 Capitulation under challenge | P3.2 |
| B7 Mode dead-end | P4.1 |
| B8 Sources rail | P4.3 |
| B9 Jurisdiction default | P3.4 |
| B10 Depth | P3.1 |
| B11 Interpretation | P3.3 |
| B12 Corpus gap | P2.4 (disclosure), P5.2 (corpus) |
| B13 Lost turns / blanks | P4.2 |
| B14 Link granularity | P1.4, P1.6 (enforcement) |

---

## Merging back

`main` is the deployment branch and the target pulls `origin/main`.

**Nothing is pushed to `main` until the whole plan is concluded (user decision, 2026-09-15).**
~~Waves are independently mergeable and should be merged as they complete rather than held to
the end.~~ That was the original policy and it is no longer in force: the branch is completed
in full and merged once, in a single push.

**Consequence a session must not talk itself out of.** The target keeps running the pre-pilot
code until that push, including B2 — measured at P1.5 as emptying 351 of 974 filtered searches
and costing 28% more and 38% longer per query. That is understood and accepted; it is not an
argument for pushing Wave 1 early, and a later session should not re-open it. If the delay
becomes untenable, raise it with the user rather than deciding unilaterally.

**"Concluded" means Waves 0–4** — the code work. Wave 5 is external, has unknown lead time,
and the plan already states those rows are not blockers for anything in Waves 0–4; they must
not hold the merge hostage to a third party's reply.

The frontend build rule still applies: any change under `client/src/` needs `npm run build` and
a force-added `client/dist/` in the same commit (see the root `CLAUDE.md`).
